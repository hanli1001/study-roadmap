.claude/
.idea/
__pycache__/
.playwright-mcp/

# ── 验证证据 / 截图（不进仓库：占空间，且随时可重新生成）──
_shot-*.png
交互演示/_shot-*.png
打印资料/_预览-*.png

# ── 改前备份（有历史价值但不该进仓库：需要时看本地 _备份/ 目录）──
_备份/
*.bak-*
*.bak

# ── 运行产物 ──
.pytest_cache/
rag/_run-*.txt
样例代码库/_run-*.txt

# 教材参考副本（259M 浅克隆，不进仓库；只保留提取出来的关键原文）
_ref/ai-agent-book/
_ref/_card_check.js
_ref/_run-book-exp11.txt
_ref/*.pdf
职业探索/_boss-*.json

# ── 导师项目相关材料（2026-09-23 补，W-41）──────────────────────
# 本仓库经 `gh repo view` 实测是 **PUBLIC**。导师明说过那套智慧养老系统是
# 「我的一个科研项目」，下面这些文件含**他项目的算法实现与内部讨论**，
# 一旦公开 = 泄露导师 IP。**无论仓库以后改成什么可见性，都不要删这两行。**
导出/
交互演示/导师答疑准备-*.html
# 物联网线的工具脚本也一并挡掉：它写着导师项目的算法函数名与数据字段（W-41 同类）
tools/trace_iot_demo.py
