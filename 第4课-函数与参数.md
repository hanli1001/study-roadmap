# 第 4 课 · 函数与参数（教学卡片）

> 配套：`teaching-methodology-v3-交互式微课.md`（教学法 v3）· `教学进度看板.md` · 交互卡 `交互演示/函数与参数-详解.html`
> 时长：**20 分钟微课（可碎片化，可拆成 2×10 分钟）** · 前置：第 3 课（数据模型）已毕业
> 素材：**学员本人 `prescription_db.py` 里的 `link()`**（真实代码，不是编的例子）

---

## 本课要打的靶子

第 3 课解决了"数据怎么存、怎么拼"（关系模型 + JOIN）。本课解决"**代码怎么组织**"：
学员已经**写过**带参数的函数（`link(fangji_name, ingredients_dosage)`），但属于"动作记忆在、概念命名碎"——
所以本课不是教他写函数，而是**给他已有的动作贴上名字**，并补齐 4 种参数类型。

**本课 ≤3 个新概念**（认知负荷红线）：
1. **参数绑定的三种来源**（位置 / 关键字 / 默认值）
2. **`return` vs `print`**（给程序 vs 给人看）
3. **作用域**（函数内的变量外面看不见）

> `*args/**kwargs`、默认参数可变对象陷阱 → **只作"见识"**，不列入必会。

---

## ① 闭卷快问（3 分钟）—— 前两课检索

1. 多对多为什么必须用第三张表？（一句）
2. `ON` 和 `WHERE` 的区别？（配对 vs 筛选）
3. `COUNT(*)` 和 `COUNT(age)` 什么时候不一样？（有 NULL 时）
4. 附加：`sql-notes.md` 记的药材数和你 `COUNT(*)` 数出来的一样吗？（第 3 课留的作业）

## ② 钩子（2 分钟）：你自己写过 8 遍的东西

打开 `prescription_db.py`，指着 8 行 `link("麻黄汤", [...])` 调用问：
> "这 8 行如果不用函数，要写多少行？改一处要改几遍？"

让他自己算（生成效应），再揭晓：**函数 = 把重复动作打包成一个名字**。

## ③ 解剖（6 分钟）：给 `<span>def</span>` 各部分贴名字

- 用交互卡 `交互演示/函数与参数-详解.html` 的 ① ② 两节
- 逐行问：`def` 是什么、`link` 是什么、括号里是什么、缩进块是什么、`return` 在哪
- **点破**：`link()` 没有 `return` → 返回 `None` → 所以 `x = link(...)` 拿到的是 None（他未来一定会踩）

## ④ 参数四种（5 分钟）：交互卡的"拧参数"区

- 依次点 7 个按钮：位置 → 给全 → 关键字 → 混合 → 关键字乱序 → 少给 → 多给
- **每个按钮点之前先猜**："这次能跑通吗？每个参数的值从哪来？"
- 让他自己总结规律（再由我确认）：
  - 位置参数靠**顺序**
  - 关键字参数靠**名字**（顺序随便）
  - 默认值**不传就有**
  - 少给/多给都报 TypeError，报错信息会说清是谁的问题

## ⑤ 返回值 vs 打印（2 分钟）

- 交互卡 ⑤ 节：`print` = 给人看；`return` = 给程序用
- 现场实验：`x = link("麻黄汤", [])` 然后 `print(x)` → **None**（不是报错，是"没交结果"）
- 追问：想让调用方能拿到结果，函数里该加什么？

## ⑥ 自己写（2 分钟布置，课后做）

> 任务①：`greet(name, greeting="你好")` —— 返回问候语（**不是 print**）
> 任务②：`greet("小明")` 与 `greet("小红", "早上好")` 各调用一次并打印
> 任务③（进阶）：`add_all(*nums)` 把所有数字加起来返回

---

## 验收标准（三级）

| 级别 | 通过标准 |
|------|----------|
| **L1** | 能指出自己 `link()` 里哪些是参数、返回什么、为什么要写函数 |
| **L2** | 独立写出 `greet()`（含默认参数），并说清 `greet("小明")` 时 `greeting` 从哪来 |
| **L3** | 能讲："**为什么 `x = link(...)` 得到 None**" + "位置参数和关键字参数什么时候必须用哪种" |

> ⚠️ "我懂了" = 危险信号 → 立刻用 ④ 的"少给/多给"两道题验证。

---

## 导师自查清单

- [ ] ③ 是拿**他自己的代码**讲的，不是我编的例子？
- [ ] ④ 每个按钮**先猜后点**了吗？（生成效应）
- [ ] 有没有让他亲眼看一次 `x = link(...) → None`？
- [ ] 新概念 ≤3？（*args/**kwargs 只提一句）
- [ ] 课后 3 张复习卡 + 三个任务发出去了吗？下次课前追作业

## 附：本课的配套文件

| 文件 | 用途 |
|------|------|
| `交互演示/函数与参数-详解.html` | 主教材：签名解剖 / 7 种调用绑定演示 / return / 作用域 / 5 类报错 / 自己写 |
| `交互演示/语法速查-常用内置函数.html` | 39 个内置函数可检索表（签名/返回类型/坑）—— 课后自查用 |
| `样例代码库/` | 每学一个函数配一个四段式 demo（len/range/enumerate/sql_join 已有） |

## 附：下次课接口

- 第 5 课候选：**作用域与模块**（`import` / `__name__ == "__main__"` / 把 `link()` 抽成可复用模块）
- 或按学员卡点择一：pytest 进阶（阶段 A 的验收项）／前端快检

---

*第 4 课卡片 v1 · 2026.09.21 · 依据：教学法 v3（微课化 + 交互卡 + 先猜后讲）· 素材实测自 `prescription_db.py`*
