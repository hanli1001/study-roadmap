# 教学法 v3 —— 交互式 · 微课化 · 可打印（导师手册）

> v3 更新：2026.09.20 · 触发原因：学员现实约束暴露（**白天有学校课，上课频率不可控；遗忘快；进度拖**）
> 与 v2 的关系：**v2 的科学原理继续有效**（检索练习 / 间隔重复 / 生成效应 / 认知负荷 / 示例-问题对），
> v3 解决的是 v2 没解决的问题：**"等一节课"这件事本身成了瓶颈**。
> 配套：`逐日学习计划.pdf`（可打印）· `交互演示/*.html` · `样例代码库/` · `改动路线.md`

---

## 〇、一句话原则（v3）

> **别等课。把每个"看不懂的东西"变成一个能拧参数的网页，把每周要做的变成一张能打勾的纸。**

v2 是"把概念翻译成人话"；v3 是"**把概念变成一个你能动手拧的机器**"。

---

## 一、v2 → v3：改了什么，为什么

| # | v2 的做法 | 出问题的地方 | v3 改成 |
|---|-----------|--------------|---------|
| 1 | 30–60 分钟整节课，靠"约到时间" | 学员白天有学校课 → **约不上就一直拖**（进度慢的真正原因） | **微课化**：10–20 分钟一个单元，**不需要导师在场**也能学 |
| 2 | 概念用文字 + 类比讲 | 抽象的东西（JOIN、作用域、指针）**文字讲十遍还是模糊** | **交互式 HTML 卡片**：能拧参数、能看结果变化，抽象变可视 |
| 3 | 导师生成为主，学员等着上课 | 遗忘发生在**两次课之间**，没人管 | **可打印计划表**：提前预习 + 每天打勾，学习不依赖导师在场 |
| 4 | 一个知识点讲完就下一个 | 陌生函数**忘了用法 → 越学越慢**（学员自述） | **每学一个函数/算法 → 一个能跑的小 demo**（`样例代码库/`） |
| 5 | 教学记录散在看板/教案里 | 长会话一 compact，路线和坑就丢 | **`改动路线.md`**：决策 / 坑 / 待办显式落盘 |
| 6 | 素材形态二选一（要么 md 要么 html） | — | **按用途分**：要调参/看变化 → HTML；要复制去跑/进 git → Markdown |

> ⚠️ 我在 2026.09.20 曾把两者当成"二选一"并否掉了 HTML —— 那是**判断错误**，已作废（见 `改动路线.md` 的坑 W-03）。

---

## 二、五条学习科学原理（沿用 v2，此处只写"v3 怎么落地"）

1. **认知负荷**：一次只装 2–3 件新东西 → 微课单元 = **1 个新概念 + 1 个可跑 demo**
2. **检索练习**：主动回忆 > 重读 → 交互卡片里**先猜后揭晓**（猜行数、猜输出），计划表每天 1 道闭卷题
3. **间隔重复**：1-3-7-30 → **打印表上的打勾格**（纸质打勾比 App 更容易坚持）
4. **生成效应**：先猜后讲 → 交互卡片必须有一个"**先猜**"输入框，猜完才出答案
5. **示例-问题对（Worked Example）**：完整示例 → 立即复刻 → 卡片里"**你先自己写**"自由输入区

---

## 三、v3 的三件武器（每个都有固定模板，不许自由发挥）

### 3.1 武器一：交互式 HTML 卡片（遇到"讲不清的抽象"就做一张）

**触发条件（满足任一即做）**：
- 学员问"**为什么**是这样"超过一次
- 概念是**过程/关系/状态变化**类（JOIN、递归、作用域、事务、索引、事件循环…）
- 一个函数**参数多、行为随参数变**（`range`、`enumerate`、切片、`zip`、正则…）

**固定结构（6 块，缺一不可）**：

| # | 区块 | 作用 | 依据 |
|---|------|------|------|
| 1 | **先猜** | 让学员先填一个预测，再揭晓 | 生成效应 |
| 2 | **能拧的参数** | 滑块/开关/增删行，**至少 3 个可调** | 主动学习 |
| 3 | **并排看变化** | 参数变 → 结果区同步变（不是重跑一次才看到） | 即时反馈 |
| 4 | **看得见的过程** | 中间步骤高亮（哪一行被配对、为什么） | 减轻负荷 |
| 5 | **可复制的代码** | 生成对应真代码，学员复制去跑一遍 | 迁移 |
| 6 | **你先自己写** | 留一个空白输入区，学员自己写、再对照 | 生成 + 检索 |

**落地位置**：`交互演示/<主题>.html`（单文件、无依赖、离线可用、手机能开）

### 3.2 武器二：可打印资料（对抗"等课"和"遗忘"）

**所有要打印的东西一律产 PDF**（学员要求，2026.09.20）。

| 资料 | 内容 | 更新频率 |
|------|------|----------|
| `逐日学习计划.pdf` | 每天 10–20 分钟：学什么 / 记什么 / 打勾格 / 卡点栏 | 每周一次 |
| `离线速查卡.pdf` | 一页 A4：常用函数 + 语法 + 排查口诀 | 每月一次或按需 |

**固定要素**（缺一不可）：日期栏 · 打勾格 · 预计耗时 · **卡点记录栏** · 下一天预告（满足"提前告知/预习"）

### 3.3 武器三：样例代码库（每学一个新函数/算法 → 一个能跑的 demo）

**铁律**：不写完 demo 不算学完。**先说清输入输出，再写代码。**

**落地位置**：`样例代码库/<主题>.py`，模板固定：

```python
"""<函数/算法> 样例 · 对应 交互演示/<xxx>.html

用法：python 样例代码库/xxx.py
"""
# ① 最小可运行：一行能看出它干什么
# ② 输入 → 输出（改参数看变化，和 HTML 卡片对上）
# ③ 边界情况：空值 / 越界 / 类型不对，会怎样
# ④ 你自己写：留一个 TODO，学员补完才算过（L2 验收）
```

> 为什么固定模板：**下次遇到陌生函数，学员照着模板自己就能整理一张卡片 + 一个 demo** —— 这是本教学法要交付的**终极能力**（自我教学）。

---

## 四、每日/每周执行流程（不依赖导师在场）

```
每天 10–20 分钟（自己在宿舍就能做，不用等课）：
  ① 打开当天"交互演示"卡片 → 先猜 → 拧参数 → 看过程   （5 分钟）
  ② 跑当天"样例代码库"的 demo → 改一个参数看变化      （5 分钟）
  ③ 打印表上打勾；卡点写进"卡点栏"                     （1 分钟）
  ④ 隔天/3天/7天：闭卷回忆 5 分钟（不重读，靠回忆）    （5 分钟）

每周一次（我或你，20 分钟，可线上可异步）：
  ⑤ 收卡点 → 出下周计划 PDF → 决定本周做哪 1–2 张交互卡片
  ⑥ 更新 改动路线.md + 看板 + git 提交
```

**导师职责变了**：v2 是"讲课的人"；v3 是"**造工具 + 收卡点**"的人。课仍然上，但**不再是进度的前置条件**。

---

## 五、验收标准（沿用 v2 三级阶梯，加一条"造工具"）

| 级别 | 通过标准 |
|------|----------|
| **L1** | 能说清"这个函数/概念做什么"，照着示例跑通 |
| **L2** | 换场景独立写出来（不查资料）；能回答"参数改成 X 会怎样" |
| **L3** | 能讲给别人听 + 回答"为什么不用方案 B" |
| **L4（v3 新增）** | **能自己为这个知识点造一张交互卡片或一个 demo** —— 从"会学"到"会自己造工具学" |

---

## 六、素材与红线（不变）

- ⚠️ 中医数据**只当作业载体**，禁止讲中医语义；抽象关系换通用场景（微信群 / 选修课 / 游戏）
- 学员可投入：碎片化 10–20 分钟/天为主（白天有学校课）；英语线另算 45 分钟/天
- 报错 = 程序在说话（失败正常化）；"我懂了" = 危险信号，必须检索验证

---

*教学法 v3 · 2026.09.20 · 依据：v2 学习科学证据 + 学员现实约束（学校课冲突/遗忘快/进度拖）+ 编程学习特性（重复即肌肉记忆）*
