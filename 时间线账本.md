# 时间线账本 — 项目学习

> **本文件只记录「项目学习」这一个工作区的事。** 其他工作区的内容在它们各自的 `时间线账本.md` 里。
> 账本机制 / 开场校准脚本 / 归档 → `E:\dsh优化\时间线账本.md`。
> 口径三条硬规则：①「今天」只由系统时钟定义（Asia/Shanghai）②只写绝对日期，不写"今天/昨天/N 天前"③每条事实标来源，推断类标「未验证」。

## 教学线说明

> 本文件只记录「项目学习」这一个工作区的事。教学主看板仍是唯一权威：`E:\项目学习\教学进度看板.md`。

## 一·B、教学与学习进度（索引层 · 跨天最容易断线）

> **教学类问题开场必读**。教学线是"隔几天上一次课"的模式，与 AI 会话的断续完全对不上。

| 项目 | 位置 / 值 |
|------|-----------|
| **教学主看板（唯一权威）** | `E:\项目学习\教学进度看板.md`（五表制度） |
| 教学法 | `E:\项目学习\teaching-methodology.md`（v2 唤醒式） |
| 学习路线 | `E:\项目学习\learning-roadmap.md`（**v4 · 就业优先**）｜ 本周执行层 `每周目标.md` |
| 学员 | **韩立**，19 岁 · 安中医计科讯飞班 25 级 · 2029.06 毕业 · 目标 AI 应用工程师 |
| 角色关系 | ✅ **用户 = 学员本人**（2026-09-18 用户亲口确认：「韩立是我给自己的称呼，小刁是现实中的」）。→ 看板 / 路线图 / `01-个人能力与信息.md` 里的「韩立」**就是用户本人**，那套能力表即其真实画像；看板作者是**教他的老师/教练**（文中"我记录"指那位老师），**不是用户**。<br>⚠️ **2026-09-18 修正**：本行此前记为"用户 = 老师/教学方案设计者…韩立是学员，不是用户本人"，属**错误推断**，已作废 |
| 教学红线 | ⚠️ 中医数据只当作业载体，**禁止讲中医语义**；抽象关系换通用场景（微信群/选修课/游戏） |
| 教学节奏 | 每次 30–60 分钟，碎片化不固定 |

**当前断点**（最后核实 2026-09-14 旧口径 ≈ 真实 09-16）：

- 看板最后写入 **2026-09-07 01:28**，但此后仍有学习活动会话（09-09 12:59 在 `E:\项目学习`）
  → **看板落后于真实进度，需向用户确认 09-09 那次做了什么，再补写表 2.2 / 表 5**
- **第 3 课（数据模型 + JOIN）状态是"待开"** —— 看板记录的下一步；P-009 是 🔴 未解决
- **复习日程表已逾期**：表 2.3「1-3-7-30」仅填了"当天 ✅ / 隔天 ⏳"；第 1、2 课隔天/3 天/7 天三格全空 → 需重新安排或标注放弃
- 卡点：P-009（JOIN 关系模型，🔴）、P-011（行业方向未定，🔄 2 周 3 行业探索）

---

## 二、本工作区流水（倒序追加）

| 日期 | 星期 | 做了什么 | 产出 |
|------|------|----------|------|
| 2026-09-23 | 周三 | 🔁 **修好「1-3-7-30」复习机制（T-24 / P-015 关闭）—— 产出 `交互演示/复习台.html` + 4 页 A4 打印版**。① **先想清楚病根**：旧版只是一张**没日期的表**，放在那儿 9 天没人管 → 这次的要害不是"多写一张表"，是**让表自己会走路**。三条设计差别：**日期算出来**（`LabProgress.review` 从起算日 2026-09-23 推出 **09.24 / 09.26 / 09.30 / 10.23**，永远不过期变废纸）· **默认只显示今天该过的**（打开就一个动作）· **逾期红字跳出来**（烂账藏不住）② **起算日为什么是 09-23 而不是各课原学习日**：第 1、2 课（原 09-06）的 1/3/7 三档**全部逾期且从未做过**，09-23 当场重测证实已忘光 ⇒ **重学即重新起算** —— 旧账不勾销（页面里写明了），但也不挂着吓人。这是诚实做法，不是把账一笔勾销 ③ **11 张卡**：第 1 课 4（`=`vs`==`/装饰器/推导式/`*`vs`**`）+ 第 2 课 3（**404 三步排查**/`commit`vs`close`/Pydantic 422）+ 第 3 课 4（绳子表/`ON`vs`WHERE`/行数=配对成功次数/`ON` 接反的两种后果）。答案全部**点开才显示**（检索练习），「没过，再来」把卡**退回待过状态**，勾选存 localStorage ④ **实测（把系统日期伪造到 09-26 跑）**：今天 = 9月26日（周六）· 本轮 = 3 天档 · 11 张 / 已过 0 → 点「看答案」答案出现（含 `<b>`）→ 标 2 张「会了」→ 11 变 9、已过 2 → 标 1 张「没过」→ 红框标记出现 → **刷新后状态全在** → 顶部逾期区报**「有 11 格过了日子还没打勾，最老的一格逾期 2 天」** ⑤ 🔴 **我自己踩的坑（W-42）：第一版打印出来的 PDF 里一个答案都没有** —— 因为卡片区**跟着"今天"走**，而生成 PDF 那一刻（09-23）**恰好不是复习日**，页面显示"今天不用复习"，一张卡都没渲染。**屏幕上看着好好的，打出来是废纸**。修法：加 `.print-only` 全量问答块（屏幕 `display:none`，打印才出现）+ `.no-print` 标记跟随日期的那两块；复测 PDF **4 页 A4（595×841.9pt）**，`下单/迎宾员/菜单/绳子/WinError/配对成功` **全部 True** ⑥ **入口打通**：`全局思维导图.html` 与 `_卡片模板.html` 的复习卡段都加了「打开复习台」链接 —— 从他实际会打开的页面进得去，不是靠记住一个文件名 ⑦ **验证**：0 console error · 桌面 1280 与手机 390 双档横向溢出 **0** · 按钮触控高度全部 ≥32px · 打印版逐页看过 | **`交互演示/复习台.html`**🆕、**`交互演示/复习台.pdf`**🆕（A4 **4 页**）、`交互演示/_全局进度.js`（新增 `LabProgress.review`：11 张卡 + 4 个算出来的复习日 + JOIN 诊断三步）、`交互演示/_主题.css`（`.answer` / `.print-only` / `.no-print` / 打印展开答案）、`交互演示/全局思维导图.html` + `_卡片模板.html`（加入口）；截图 `_shot-review-desktop.png`/`_shot-review-mobile.png`/`_shot-review-pdf-1.png`（均已被 `.gitignore` 覆盖）；`改动路线.md`（T-24 ✅ / **W-42**）；`教学进度看板.md`（P-015 ✅） |
| 2026-09-23 | 周三 | 🎓 **第 3 课 ①–④ 实际上课（P-009 挂半个月后终于开课）+ 教学材料体系升 v2**。① **开场校准时间**：22:46 开课，只上前半段（教案本就按"可断成 2 次"设计），⑤⑥ 留到下次 ② **① 闭卷检索**：`=`/`==` 半对（方向对，差"有没有副作用"这层）、404 三步排查**忘了**（09.06 学，17 天）、`conn.close()` **答错**、三张表**对** ③ 🔴 **我自己先犯一错并当场纠正**：说"6 个空"，实际摆的是 **8 个** ④ 🔴 **教案假设被实测推翻**：教案 09-20 写「语法他会、断点在关系模型」——**正好相反**。他答对"三张表"，还独立写出正确的 `ON 群.id = group_members.gid`；卡住的是**语法骨架**（`FROM JOIN` 后缺表名、只 JOIN 一次、漏 `WHERE`）。**填空 7/8，语法零错误** ⑤ **实跑验证**：他的 SQL 一字不改跑出 3 行；补上漏掉的 `WHERE groups.id = 1` → **2 行**（`第3课-实跑输出-20260923.txt`）⑥ **ON 写错的两种后果现场演示**：接反 → **0 行不报错**；id 重叠 → 返回"看着合理"的 3 行假数据 ⑦ 🔴 **当场实测纠正 `commit` vs `close`**：`commit✅/close❌` → 另一连接**读到 1 行**；`commit❌/close✅` → **0 行**。⇒ **丢数据的是 `commit` 不是 `close`**（他没关的连接还真锁住了我的测试文件，WinError 32）⑧ 🔴 **挖出真正的病根**：账本 09-14 就记过「1-3-7-30 复习日程表逾期」，**空转 9 天没人管** → 404 会忘不是记性差，是机制没跑（记 P-015 / T-24）⑨ 🎨 **应学员要求做教学材料体系 v2**（四个要求：护眼方格纸底 / 交互更好 / 抽象处加动画 / 每份课前 HTML 带思维导图 + 一份全局思维导图）—— 走 impeccable 流程：写 `PRODUCT.md` → `concept-seed` 滚出方向（实验记录本）与用户钉的「米黄方格纸」同族 → 合成一个世界 ⑩ 产出 5 个文件（见产出列）⑪ 🔧 **量出来的四个真 bug**：`Lab.rail()` **漏加 `.rail` 类**（CSS 静默失效 → 旗子压正文 6px + 手机整页溢出 **132px**）、grid 子项 `min-width:auto`（`<pre>` 顶宽 **53px**）、令牌压在数据上（`overlapArea` 1079px² → 加弓形连线 + 48px 走线道 → **0**）、HTML 里写了 **14 处** Markdown `**加粗**` ⑫ ⚠️ **第三次把渲染伪影当 bug**：令牌的 `→` 看着像 `✗`，放大 3 倍才看清是**连线从字上碾过去**（记 W-39）⑬ **机械检测器 19 → 7**（修掉低对比 8 / 字阶 2 / 彩色左边框 1 / 动画 width 1；剩 7 条全是**用户明确定死**的米黄+方格，属"brief 压倒默认"，不是缺陷）⑭ 🔴 **提交前拦下一次真泄密**（W-41）：`导出/物联网Demo-给导师-*.md` 含**导师项目的 main.py 全部源码**，而远端 `hanli1001/study-roadmap` 实测仍是 **PUBLIC** → 已加 `.gitignore` 并复核命中 | **`交互演示/_卡片模板.html`**（v2 重写）、**`交互演示/全局思维导图.html`**🆕、**`交互演示/_主题.css`**🆕、**`交互演示/_学习工具.js`**🆕、**`交互演示/_全局进度.js`**🆕、`PRODUCT.md`🆕、`第3课-实跑输出-20260923.txt`🆕；截图 `_shot-global-desktop.png`/`_shot-template-mobile.png`/`_shot-pair-desktop.png`/`_shot-template-mm.png`/`_shot-rail-desktop.png`（均被 `.gitignore` 覆盖）；看板 P-009 重写 + 新增 **P-014**（commit/close）**P-015**（复习机制空转）+ 表 2.1/3.2；`改动路线.md` 新增 **W-37…W-41** + T-01 更新 + **T-24/25/26**；`每周目标.md` 日期校准 |
| 2026-09-23 | 周三 | 💬 **导师回复「找个时间点仔细看看」→ 产出答疑准备；过程中我推翻了自己一个错误结论**。① 这句话**主语省略**，两种读法（他自己抽空看 / 约一次一起看）→ 给了一段**对两种读法都成立**的回复草稿（不催、不问"您看了吗"，同时表明随时可配合）② 产出 **`交互演示/导师答疑准备-20260923.html`**（13.8 KB）：这句话怎么读 + **8 个「他可能问」的问题**（分「必答对 / 要能答 / 好答」）+ 3 条通用原则 ③ 🔴 **关键实测：`select_service` 的「时段加分」是常数，零区分作用** —— 上面已 `continue` 把时段不合适的筛掉，走到加分的幸存者个个满足 ⇒ 对所有人无条件 +1.0；**去掉它，18 条老人里 0 条排序变化**。这是导师「仔细看看」时一眼能看出的无效项 ④ 🔴 **我自己先犯了一次错并当场纠正**：起初用「把价格权重 1.0 放大到 2.0 看排序变不变」测价格项，得出「价格项从未起作用」—— **无效设计**（放大一个本来就在区分次序的项，当然不改次序）。改成「**去掉它再看**」重测：**去掉价格项 → 18 条里 11 条排序变化** ⇒ 价格项**恰恰是同标签数服务之间唯一的排序依据**。错误结论与纠正过程**原文保留，未删** | `交互演示/导师答疑准备-20260923.html`🆕、`交互演示/_shot-qa-mobile.png`（已 gitignore）；导师口径见 `E:\物联网\时间线账本.md` |
| 2026-09-23 | 周三 | ✅ **物联网 Demo 已发出（T-17 关闭）+ 产出「今日清单」交互页**。① 学员确认「已经发过去，等回复」—— **4 天零信号结束**；T-17 转 ✅，新增 **T-23「等导师回复」**（回复一到立刻更新 `每周目标.md` 🔵 段，可能重排本周主轴；3 天无回复就主动跟进）② 产出 **`交互演示/今日清单-20260923.html`**（23.2 KB，W39 第 3 天）：**顶部可勾选清单**（4 项 + localStorage 持久化 + 进度条）· **按时间切**（课间 10 分钟 / 回宿舍整块 / 睡前 20 分钟）· **JS 按 h2 自动生成的跳转目录**（不手写，改标题不会忘同步）· **三组卡片共 10 条**（Linux / SQL+Python / LeetCode）③ **卡片严格按 `_卡片模板.html` 的「3 秒判断」分类** —— 只给真正够格的标「值得做真卡片」（grep 参数、`2>/dev/null`、管道、du 管道三连、sqlite3 四步、`float("inf")`），"名字记不住"的一律标「看一眼就懂」，**没有为了凑数把速查项硬做成卡片** ④ **所有命令输出都是本机真实实测，不是编的**：`grep "8009"` → **2 条**（含小数 `0.800927443` 的假命中）vs `grep -w` → **1 条**；`grep -rn` 递归 **8 条命中全是 `.venv` 噪音**、加 `--exclude-dir` 归零；`du -sh */` 管道三连真实输出；`grep -rn … \| wc -l` → 113 ⑤ **验证**：`node --check` JS 通过 · 标签全配平 · Playwright 真点 **9 项**（grep 命令实时拼装 + 取消、进度 0→25%→50%、`done` 类生效、localStorage 刷新后仍在）· 手机 390px **零横向溢出**（逐元素扫描）· 目录 6 条锚点全对且真跳转（scrollY 200→6456）⑥ ⚠️ **又一次差点把缩放伪影当 bug**：预览里「不占今天的时间」下像有条下划线 → 逐元素量 `text-decoration` 全 `none`、下边框 0px，全页扫到的"下划线"**全是正常 CSS 边框** ⇒ **未做无谓改动**（与 09-22 那次"文字发蓝"同一类） | `交互演示/今日清单-20260923.html`🆕、`交互演示/_shot-today-mobile.png`、`_shot-today-desktop.png`（两张截图均已被 `.gitignore` 覆盖）、`改动路线.md`（T-17 ✅ / T-23 🆕）、`每周目标.md`（🔵 段状态刷新，4 处旧文案清零） |
| 2026-09-22 | 周二 | 📤 **导出「给导师看」的材料（两版）+ 修掉 `main.py` 一个潜在缺陷 + 自己又踩一次 `edit` 锚点坑**。① 写 `tools/export_demo.py`：用 `tokenize`+`ast` 两趟（都基于原始行号）去注释与 docstring → 拼入两个 JSON 全文 → **自测：去注释后实跑输出与原文件逐字节一致（15,594 B / rc=0）**；产出全文版（647 行 / 22 KB）与**节选版（234 行 / 7.7 KB，建议主发，因为 645 行微信里没人读得完）** ② 🔧 **学员授权后修 `E:\物联网\demo\main.py`**：`combine_by_cost` 缺去重 —— 同一服务命中多个需求且都是最低价时会被加进方案**两次**（`combine_by_priority` 有 `used`，它没有）。**先验备份可作"旧版"基准（sha256 一致）再动手**；回归三项：**人造用例逼出缺陷**（旧 `['X1','X1']` → 新 `['X1','X3']`，且断言"旧版没复现就说明用例无效"）、真实 20 条样本改前改后 **0 差异**（修复零副作用）、`main.py` 退出码 0 ③ ⚠️ **顺带发现一处学员需能向导师解释的不一致**：方案A 按**服务类型**去重、方案B 现按**服务 ID**去重，且两者 docstring 都没写 ⇒ 记 T-22 ④ 🐛 **自省（W-36）**：给 T 表追加行时拿"下一行行首"当锚点，把 T-22 缝进了 T-21 中间 —— 靠 grep 数行数发现并修复；已立规矩"追加用**上一行**或表头做锚点，改完数 `\|` 个数" | `tools/export_demo.py`🆕、`导出/物联网Demo-给导师-20260922.md`🆕、`导出/物联网Demo-给导师-节选版-20260922.md`🆕、`E:\物联网\demo\main.py`（已修，回归通过）、`改动路线.md`（W-36 / T-22） |
| 2026-09-22 | 周二 | 🔀 **学员要求「物联网工作区与当前工作区合并」→ 我先做安全核查，最终定为「只在计划层打通，物理不合并」**。① 🔴 **核查拦下一个真风险**：`E:\项目学习` 的远端 `hanli1001/study-roadmap` 经 `gh repo view` 实测是 **PUBLIC**，而 `E:\物联网` 里有 4 个文件**不在任何 .gitignore 保护下** —— 同学**许文涛的整本本科毕业论文 docx（4.5 MB，含姓名/学号/指导教师名）**、论文全文提取（112 K）、导师的选题材料（他明说是"我的一个科研项目"）、与导师的私人沟通草稿。**若直接合并，一次 `git add .` 就是学术不端 + 侵权 + 泄露导师 IP** ② 学员选**不物理合并** ⇒ 敏感件本就在仓库外，**风险自然为零**，无需 gitignore 补丁 ③ **做了完整备份**：`E:\物联网_备份_20260922`（116 文件 / 9.6 M，`diff -r` 逐字节一致）④ **补上 W-33 的结构性修复**：把「这个学员一共有几条线」的索引搬进 `改动路线.md` **第〇节**（本文件是 compact 后先读的那份）→ **读到这里就一定会看到全部线，不再靠记性** ⑤ 记录：`E:\物联网` 当时**正在被改动**（21:43–21:49，账本 21,980 → 24,329 字节），故先取得一致快照再备份。**产出**：`改动路线.md` 新增第〇节「工作区线索引」+ W-35 + T-21；备份目录 `E:\物联网_备份_20260922`。**边界遵守**：**未改动 `E:\物联网\` 下任何文件**（它正在被写，且有"不回写"声明），跨向引用只从本工作区一侧发起 | `改动路线.md`（第〇节 / W-35 / T-21）、`E:\物联网_备份_20260922`（新） |
| 2026-09-22 | 周二 | 🔵 **学员告知「`E:\物联网` 也算我的一部分，那是导师给的任务」→ 补回一整条被漏掉的工作区线**（记 W-33）。核实后：① 该线**早已登记在** `E:\dsh优化\时间线账本.md` 的全工作区索引里（`E:\物联网` = 毕设/导师任务/毕业论文），**我有索引却没读** ② 任务 = 完善学长毕设《面向多模态感知的智慧养老原型系统》的三个算法，**是导师本人科研项目的落地**，导师**据此推荐实习** ③ 🔑 **难度已大幅下调**：导师第五次口径「本科生**不要求创新**，用现成算法把流程实现一遍」→ 只需 pandas/sklearn/大模型 API/pulp/Streamlit 的**调用级**用法，**不需要深度学习**；结论由「缺 3–6 个月地基」修正为「**现在就能开始**」 ④ 最小 Demo 09-20 已搭出并实跑通过，**但 2 天没给导师看** —— 这是本周最高性价比的动作（T-17）。**产出**：`learning-roadmap.md` 增「🔵 第二条线」+ 合并/并行待决表 + 毕设排期；`每周目标.md` 增「🔵 物联网线（独立节奏，不占 3 个名额）」；看板 4.1 增「实习通道=导师推荐」+ 4.5 增 3 条待办；`改动路线.md` 新增 W-33 与 T-17/T-18/T-19。**边界遵守**：未改动 `E:\物联网\` 下任何文件，也不把该线细节写进本工作区账本（跨工作区隔离） | `learning-roadmap.md`、`每周目标.md`、`教学进度看板.md`、`改动路线.md`（W-33 / T-17–19） |
| 2026-09-22 | 周二 | **学员决定「考研当备选，找工作在前」+ 要求「以后每周一更新，一周学习的要点 = 当前周要完成的学习目标」→ 路线升级 v4「就业优先版」并新增执行层**：① 先核对，发现该决定**与看板 4.1 原记录一致** —— 是**把默认选项升级为明确决定，不是方向改变**（如实说明，没当新闻写）。② 建立**倒推链条**作为整份路线的新骨架：2029.06 毕业 ← 秋招 2028.09–11 ← 暑期实习 2028.07 ← **分岔口 2028.03** ← 第一段实习 2027.07 ← **2027.03 简历上必须有东西** ← 2027.01 作品 v1 ← 2026.10 立项 ← **现在只剩 23 周**。③ 🔴 **v4 修掉 v3 的第一个错**：v3 把 RAG 系统排到 2027.06 才上线 → **2027 春投实习时简历是空的**；⇒ **项目前置**到 2026.10 立项 / 2027.01 上线公网，阶段 A 主目标由「算法 150 题」改为「**自己写的 AI 应用 v1**」，并把「我写你看」翻转为「**你写我审**」。④ 算法 150 降为「每天 1 题」的**地板**；Java 由「大二暑假主目标」降为**可选**（`[实测]` 本科可投 17 条 JD 里只有 2 条要 Java）。⑤ 🔴 **修掉 v3 一句被高估的话**：逐科比对后「双轨准备（无浪费）」**不成立** —— 就业线只覆盖 408 的 **2/4**（缺组成原理、操作系统），且**考研数学完全不在线上**；⇒「考研备选」真实成本 90% 在数学；**最便宜的保险是免费的**（学校的高数/线代/概率/操作系统课认真上 = 零额外时间成本）；并明确 **2028.03 是分岔口**（秋招 2028.09–11 与考研初试 2028.12 只差 3 个月 → 实为二选一，零基础临阵转考研不可能）。⑥ 新增 **`每周目标.md`**（滚动文件，每周一重写）+ `周目标存档/_模板.md`，机制固化进 `改动路线.md` 五·0.7（**≤3 件事** / 验收必须**可验证** / **必带「本周明确不做」栏** / 上周 ❌ **不许原样再列**）；W39 = 09.21–09.27，本周三件事 = Linux 微课 1 + 项目立项 + 第 3 课 JOIN。⑦ 产出可打印 **`打印资料/教学路线图-就业优先.html`+`.pdf`**（A4 **1 页**，以倒推链为脊柱）。**实测验证**：HTML 标签全配平 · PDF **1 页**（PyMuPDF 实测 595×841.9pt）· 正文最低点 780.3pt（下边距 21.7mm）· 特殊符号 ▶✕✓⇒⏰← **全部落入 PDF 文本**（无缺字）· 墨色实测**蓝通道污染仅 3/230649 像素（0.001%）** —— 此前从缩略图怀疑「一段文字发蓝」，**实测证伪，是缩放伪影**，正文墨色 `#13171A` 与设定一致，**故未做无谓改动** | `learning-roadmap.md`（**v4**）、**`每周目标.md`**🆕、**`周目标存档/_模板.md`**🆕、**`打印资料/教学路线图-就业优先.html`+`.pdf`**🆕、`打印资料/_预览-路线图-就业优先-1.png`；`教学进度看板.md` 升 v5（表 1.1 / 1.2 / 4.1 / 4.2 / 4.3 + 每周一更新机制）；`改动路线.md`（变更记录 + 五·0.7 周更机制 + T-14/T-15/T-16 + 第一节文件体系） |
| 2026-09-21 | 周一 | **开 AI Agent 学习线**（学员给链接 `github.com/bojieli/ai-agent-book`，说"这个 agent 学习"）：先读原仓库原文（README / docs/zh-CN/LEARNING.md / chapter1–3 README）建立事实，再**亲手验证**而非纸上规划。① 写 `样例代码库/agent_上下文消融.py`：本机 Ollama + qwen2.5:3b 复刻书里实验 1-1 的"上下文消融"思想，5 个消融组 × 4 题 × 5 重复 = 100 次模型调用，**判分由代码做**（不靠感觉）。实测矩阵：完整 20/20 → 砍掉工具定义 0/20、砍掉工具结果回灌 0/20、**砍掉系统提示仍 20/20（反直觉，两轮复现）**、砍掉历史 15/20（只伤两步题，那列 5/5 5/5 5/5 0/5）② 做交互卡片 `交互演示/Agent核心公式-上下文消融.html`（4 个可拧开关实时改变"模型看到的上下文"+ 真实轨迹 7 步回放 + 实测矩阵 + 两个坑），Playwright 实测交互 6 项 + 视觉复核 3 张截图 ③ 产出可打印 `打印资料/AI-Agent学习路线图.html/.pdf`（2 页：十章难度地图 + 你的水平线 + 三条入口 + 三条实话 + 本机环境 + 坑 + 诚实标注的不确定项）④ **把书里官方起点实验 1-1 在本机跑通**（`--provider ollama --model qwen2.5:3b` → 注册 4 工具、自主调 `calculate`、答对 2000），证明"不买 API key 也能学这本书" ⑤ 修 `tools/html2pdf.py` 两个真 bug（`as_uri()` 编码 URL 让 Edge 静默不出文件；落盘轮询只等 6 秒误判成功为失败）——影响**所有**可打印资料 | `样例代码库/agent_上下文消融.py`、`样例代码库/_run-agent消融.txt`、`交互演示/Agent核心公式-上下文消融.html`、`_shot-agent-top/mid/bottom.png`、`打印资料/AI-Agent学习路线图.html`+`.pdf`、`_ref/ai-agent-book-README.md`、`_ref/LEARNING-zh.md`、`_ref/_run-book-exp11.txt`、`_ref/ai-agent-book/`（浅克隆+sparse）；看板新增 2.1.1 节 + 2.1 两行；改动路线新增 W-25…W-29 与 T-12/T-13 |
| 2026-09-21 | 周一 | **git 留版本节点（5 个 commit + 推送 GitHub）**：把 9/20–21 两天的积累整理成 5 个主题提交并推送 `hanli1001/study-roadmap`。提交前先做仓库整理：6 个改前备份移入 `_备份/`、更新 `.gitignore` 忽略截图（`_shot-*.png`）与运行产物、确认 `prescriptions.db`（内容未变，仅 SQLite 页重写）可安全提交 | 5 个 commit：`5c40425` 教学体系 v3（21 文件/4490 行）· `26b1b39` 算法模式①②（8/1081）· `6c2e7f5` pytest+教案（6/779）· `ec4c717` RAG 打通（4/479）· `2ec1e35` 登记文档+整理（11/491）；合计 **49 文件 / +7320 行**；`main` 已推送并同步 |
| 2026-09-21 | 周一 | **双指针模式过关（25/25）** —— 学员第一个"自己写代码 + 自己跑测试 + 自己修 bug"的完整闭环：过程中修掉 3 个真 bug（`max_area` 死循环 / 用 `max` 当水位（应为 `min`）/ 两指针一起动跳过组合；`move_zeroes` 的 pop 索引问题）。学员的 `move_zeroes` 用的是"pop+append+预算控制"的**非标准第二解**，逻辑自洽且通过全部用例。**关键流程改进（学员要求）**：立"跑代码前先静态三查"规矩（循环变量是否推进 / 终止条件 / 有没有破坏题目前提）+ 给判分器加跨平台超时护栏（W-23 修 `signal.SIGALRM` 的 Windows 坑） | `样例代码库/two_pointer_自测.py`（25/25，含超时护栏）、`tools/verify_pointer_grader.py`（判分器自检工具，用权威正解验证且自动恢复原文件）；改动路线新增第五节 0.5「静态三查」+ 坑 W-23/W-24 |
| 2026-09-21 | 周一 | **哈希表模式验收通过（L2）→ 交付模式② 双指针**：① 把验收做成**自助工具** `hash_pattern_自测.py`（4 变式 + 判分），学员三轮修改后 **23/23 全绿** ② 学员最终解法已存档（`set` 判重 / `dict.get` 计数 / `need in (x-k, x+k)` 双向查 / 遍历原串保序）③ 新增 `交互演示/算法②-双指针.html`（与哈希表的分工表 / "一次比较排除一整片"的灵魂推理（含故意写错的推理让学员自己抓）/ 指针动画 / 三变式含盛水与移动零 / 6 处常见错）+ `样例代码库/two_pointer_自测.py`（4 题 25 用例，**判分器已用权威正解验证全绿才交付**） | `样例代码库/hash_pattern_自测.py`、`交互演示/算法②-双指针.html`、`_shot-algo2.png`、`样例代码库/two_pointer_自测.py`；看板 2.1 算法行更新为"模式①②" |
| 2026-09-21 | 周一 | **哈希表模式 → 自测验收件**：学员自述"哈希算法我已经学会了" → 按教学法"我懂了是危险信号"的规矩，把验收做成**可自助的工具**（而不是等我批改）：写 `样例代码库/hash_pattern_自测.py`（4 道同模式变式 + 18 个用例自动判分 + 三级自救提示 + 参考答案）。**交付前自检抓出我自己出题的错**（W-20）：`two_diff` 两个用例期望值写错（该题有多解，正解是按顺序扫到的 [1,2]），用参考答案跑判分器才暴露 → 修正后期望值全绿 18/18 | `样例代码库/hash_pattern_自测.py`（9049 bytes，交付状态=4 题待写）；改动路线新增 W-20（判分器交付前必须先用正解跑一遍） |
| 2026-09-21 | 周一 | **RAG 从"看懂"到"跑通+可量"（方向 3 第一次真跑模型）**：bge-m3 下载完成后① 写 `rag/01_embedding_检索.py`：真 embedding 检索（1024 维，8 条文档现算 4.49s=562ms/条，向量缓存；问题向量化首调 2.2s 冷加载→之后 16–39ms）。实测**6/6 问题 top-1 都是四君子汤**（含字面 0 命中的「免疫」「免疫力差」「体虚乏力」「增强体质」）② 写 `rag/02_rag_完整流程.py`：检索+生成（qwen2.5:3b）。**受控实验抓出真问题**：我最初的严格提示词导致"检索对了模型却说资料里没有"→ A/B/C 三版对照后改成"鼓励用资料+保留不编底线"，四项全过（含「能治癌症吗」→ 正确答"资料里没有提到"不编造）③ 写 `rag/03_评估检索质量.py`：15 条测试集 + Recall@k/MRR/命中率 → **Recall@1=87%、Recall@3=100%、MRR=0.933**，并列出 2 个失败案例（怕冷又出汗→白虎汤盖过桂枝汤；发高烧口渴→银翘散盖过白虎汤）作为下一步改进基线 | `rag/01_embedding_检索.py`、`rag/02_rag_完整流程.py`、`rag/03_评估检索质量.py`、`rag/_run-01.txt`、`rag/_run-02b.txt`、`rag/_run-03.txt`、`rag/_embeddings-cache.json`；改动路线新增 W-19（提示词过防御导致 RAG 装死） |
| 2026-09-21 | 周一 | **方向 3（AI 应用 / RAG）纳入，并完成起步**：学员明确"选项 3 也是方向" → 三线并进。① 先探本机：**Ollama 0.34.2 已装且在跑**，已有 `qwen2.5:3b`(LLM) / `llama3.2-vision:11b` / `minicpm-v:8b`，**缺 embedding 模型**（已后台拉 `bge-m3`）② 用学员真库实测"字面匹配"的两个盲区（搜「免疫」「炎症」0 首；搜「热」过度命中 6 首）③ 实测证伪"零依赖伪向量"方案（字 n-gram 对「免疫/体虚乏力/增强体质」全 0 命中 → **语义检索必须上模型**，记 W-18）④ 产出 RAG 起步卡：三档检索（逐条扫描/倒排索引/向量）并排对比 + RAG 全流程"你在哪一步" + "模型只看到你塞给它的"心智模型 | `交互演示/RAG①-检索三档对比.html`、`_shot-rag1.png`；看板 2.1 新增「AI 应用 / RAG」行；改动路线 W-17/W-18 |
| 2026-09-21 | 周一 | **学员划边界 + 转向真主线（1+2）**：① 学员明确"Python 内置函数不用教学" → 关闭 T-04/T-05，写入路线文档第 0 条边界 ② **算法 150 题起步**（阶段 A 主目标）：新增哈希表模式卡（O(n²) vs O(n) 并排逐步实测，10 个数时 45 次 vs 10 次；去重/无解边界；8 处常见错；同模式 5 个变式）+ `样例代码库/two_sum.py`（真实跑分：n=20000 时暴力 12.9s vs 哈希 0.0035s，3712×）③ **pytest 收尾达成**：新建 `conftest.py`（共享缓存内存库 fixture，含"每次请求新连接"与"跨线程"两个坑的解法）+ `test_api_v2.py`（21 个测试，fixture + 参数化，覆盖全部 9 接口）→ 全套 **27 passed / 0.25s**，真库仍 8/33/49 无污染 | `交互演示/算法①-哈希表查找.html`、`_shot-algo1.png`、`样例代码库/two_sum.py`、`样例代码库/_run-two_sum.txt`、`conftest.py`、`test_api_v2.py`；看板 2.1（pytest ✅ / 算法 🔄）；改动路线新增 W-16 |
| 2026-09-21 | 周一 | **函数详解卡补齐 + 第 4 课开工**（学员：SQL 基础已完成，其他函数也要抽丝剥茧式，继续下一步）：① `range`/`enumerate` 重做成详解版（与 `len` 同构：签名 → 参数类型 → 返回类型 → 异常 → 边界 → 自己写）② 新增**可检索语法速查**（39 个内置函数，含"坑"一列）③ 新增**函数与参数**主课卡（用学员自己 `prescription_db.py` 的 `link()` 讲：为什么需要函数 / 签名解剖 / 4 种参数 / 7 种调用绑定演示 / return vs print / 作用域）④ 写 `第4课-函数与参数.md` 教案（20 分钟微课 + 三级验收）⑤ 8 张卡全量 `node --check` 语法体检通过 | `交互演示/range卡片-详解.html`、`enumerate卡片-详解.html`、`语法速查-常用内置函数.html`、`函数与参数-详解.html`、`_shot-range/enum/bifs/func.png`、`第4课-函数与参数.md`；改动路线补坑 W-14（揭晓按钮先 render 再读输入 → 永远"先填一个数"）、W-15（速查卡单引号嵌套致整页 JS 崩） |
| 2026-09-20 | 周日 | 应约读通工作区全部文档 → 备 **第 3 课（数据模型 + JOIN）**：诊断 P-009 断点（假设=关系模型而非语法）、设计"三步推导法"教案、换皮到他自己建过的 `prescriptions.db`；实测教案里每条 SQL，**证伪并修正了自己初稿的一个错断言**（"ON 写错必然空结果"——id 空间重叠时实为返回伪正确数据） | `第3课-数据模型与JOIN.md`、`第3课-微信群演示.py`、`教案SQL校验-20260920.txt`、`JOIN诊断-校验-20260920.txt`、`演示脚本-实跑输出-20260920.txt`；看板表 2.2/2.3/3.2/5.1/附录C 已更新 |

**状态备注**：第 3 课**尚未实际上课**，看板记 📋「教案就绪，待上课」；P-009 由 🔴 转 🔄（处理中）。

**实测数据修正（供后续引用）**：`prescriptions.db` 实测 **8 首方剂 / 33 味药材 / 49 条关联**；
`sql-notes.md` 第八节记的是"8 / 37 / 48"，与库不符 —— 未擅自改他的笔记，留作第 3 课让他自己 `COUNT(*)` 数一遍（检索练习）。

| 2026-09-20 | 周日 | **卡片加厚 + SQL 交互三连**（学员反馈"交互内容解释太少，要参数种类/输出类型种类；SQL 也要同样详解"）：① `len` 卡重做为详解版（签名 → 参数种类分"能数/不能数" → 交互 → 返回类型 → 4 种用法 → 5 类异常报错 → 边界 → 自己写）② SQL 新增三张分层详解卡：查询与筛选（语句骨架/NULL 坑/排序/限制/**带真模拟器**：改 SQL 真跑 + 五步执行拆解）、分组与聚合（五个聚合函数签名/COUNT(*) vs COUNT(列)/GROUP BY 分堆过程/WHERE vs HAVING/执行顺序图）、JOIN 详解（为什么拆表/三步推导/四种连接/ON vs WHERE/NULL 陷阱/错配诊断）③ 全部用 Playwright 真点验证（①8 项 ②8 项 ③5 项 ④14 按钮） | `交互演示/SQL基础①②③-*.html`、重写的 `len卡片-最小范例.html`、`_shot-SQL①/②/③.png`、`分组统计-校验.txt`；`改动路线.md` 补坑 W-11…W-13 |
| 2026-09-20 | 周日 | **教学法升级 v3（交互式 · 微课化 · 可打印）** —— 学员反馈三件事：白天有学校课约不上课 → 一直拖；陌生函数忘用法 → 进度慢；知识要可视化可调参。据此产出三件武器：① 交互演示 HTML（第一张：JOIN 配对过程，能拧参数、逐步看配对）② 样例代码库（len/range/enumerate/sql_join 四段式模板）③ 可打印 PDF（逐日计划/速查卡/记忆曲线打卡 + Edge 无头打印流水线）；另建 `改动路线.md` 防 compact 失忆 | `teaching-methodology-v3-交互式微课.md`、`交互演示/JOIN配对过程.html`、`样例代码库/*.py`(4)、`打印资料/*.html`+`*.pdf`(3+3)、`tools/html2pdf.py`、`改动路线.md` |
