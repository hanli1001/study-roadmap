# 时间线账本 — 项目学习

> **本文件只记录「项目学习」这一个工作区的事。** 其他工作区的内容在它们各自的 `时间线账本.md` 里。
> 账本机制 / 开场校准脚本 / 归档 → `E:\dsh优化\时间线账本.md`。
> 口径三条硬规则：①「今天」只由系统时钟定义（Asia/Shanghai）②只写绝对日期，不写"今天/昨天/N 天前"③每条事实标来源，推断类标「未验证」。

## 教学线说明

> 本文件只记录「项目学习」这一个工作区的事。教学主看板仍是唯一权威：`E:\项目学习\教学进度看板.md`。

## 一·B、教学与学习进度（索引层 · 跨天最容易断线）

> **教学类问题开场必读**。教学线是"隔几天上一次课"的模式，与 AI 会话的断续完全对不上。

| 项目 | 位置 / 值 |
|------|-----------|
| **教学主看板（唯一权威）** | `E:\项目学习\教学进度看板.md`（五表制度） |
| 教学法 | `E:\项目学习\teaching-methodology.md`（v2 唤醒式） |
| 学习路线 | `E:\项目学习\learning-roadmap.md`（v3） |
| 学员 | **韩立**，19 岁 · 安中医计科讯飞班 25 级 · 2029.06 毕业 · 目标 AI 应用工程师 |
| 角色关系 | ✅ **用户 = 学员本人**（2026-09-18 用户亲口确认：「韩立是我给自己的称呼，小刁是现实中的」）。→ 看板 / 路线图 / `01-个人能力与信息.md` 里的「韩立」**就是用户本人**，那套能力表即其真实画像；看板作者是**教他的老师/教练**（文中"我记录"指那位老师），**不是用户**。<br>⚠️ **2026-09-18 修正**：本行此前记为"用户 = 老师/教学方案设计者…韩立是学员，不是用户本人"，属**错误推断**，已作废 |
| 教学红线 | ⚠️ 中医数据只当作业载体，**禁止讲中医语义**；抽象关系换通用场景（微信群/选修课/游戏） |
| 教学节奏 | 每次 30–60 分钟，碎片化不固定 |

**当前断点**（最后核实 2026-09-14 旧口径 ≈ 真实 09-16）：

- 看板最后写入 **2026-09-07 01:28**，但此后仍有学习活动会话（09-09 12:59 在 `E:\项目学习`）
  → **看板落后于真实进度，需向用户确认 09-09 那次做了什么，再补写表 2.2 / 表 5**
- **第 3 课（数据模型 + JOIN）状态是"待开"** —— 看板记录的下一步；P-009 是 🔴 未解决
- **复习日程表已逾期**：表 2.3「1-3-7-30」仅填了"当天 ✅ / 隔天 ⏳"；第 1、2 课隔天/3 天/7 天三格全空 → 需重新安排或标注放弃
- 卡点：P-009（JOIN 关系模型，🔴）、P-011（行业方向未定，🔄 2 周 3 行业探索）

---

## 二、本工作区流水（倒序追加）

| 日期 | 星期 | 做了什么 | 产出 |
|------|------|----------|------|
| 2026-09-21 | 周一 | **双指针模式过关（25/25）** —— 学员第一个"自己写代码 + 自己跑测试 + 自己修 bug"的完整闭环：过程中修掉 3 个真 bug（`max_area` 死循环 / 用 `max` 当水位（应为 `min`）/ 两指针一起动跳过组合；`move_zeroes` 的 pop 索引问题）。学员的 `move_zeroes` 用的是"pop+append+预算控制"的**非标准第二解**，逻辑自洽且通过全部用例。**关键流程改进（学员要求）**：立"跑代码前先静态三查"规矩（循环变量是否推进 / 终止条件 / 有没有破坏题目前提）+ 给判分器加跨平台超时护栏（W-23 修 `signal.SIGALRM` 的 Windows 坑） | `样例代码库/two_pointer_自测.py`（25/25，含超时护栏）、`tools/verify_pointer_grader.py`（判分器自检工具，用权威正解验证且自动恢复原文件）；改动路线新增第五节 0.5「静态三查」+ 坑 W-23/W-24 |
| 2026-09-21 | 周一 | **哈希表模式验收通过（L2）→ 交付模式② 双指针**：① 把验收做成**自助工具** `hash_pattern_自测.py`（4 变式 + 判分），学员三轮修改后 **23/23 全绿** ② 学员最终解法已存档（`set` 判重 / `dict.get` 计数 / `need in (x-k, x+k)` 双向查 / 遍历原串保序）③ 新增 `交互演示/算法②-双指针.html`（与哈希表的分工表 / "一次比较排除一整片"的灵魂推理（含故意写错的推理让学员自己抓）/ 指针动画 / 三变式含盛水与移动零 / 6 处常见错）+ `样例代码库/two_pointer_自测.py`（4 题 25 用例，**判分器已用权威正解验证全绿才交付**） | `样例代码库/hash_pattern_自测.py`、`交互演示/算法②-双指针.html`、`_shot-algo2.png`、`样例代码库/two_pointer_自测.py`；看板 2.1 算法行更新为"模式①②" |
| 2026-09-21 | 周一 | **哈希表模式 → 自测验收件**：学员自述"哈希算法我已经学会了" → 按教学法"我懂了是危险信号"的规矩，把验收做成**可自助的工具**（而不是等我批改）：写 `样例代码库/hash_pattern_自测.py`（4 道同模式变式 + 18 个用例自动判分 + 三级自救提示 + 参考答案）。**交付前自检抓出我自己出题的错**（W-20）：`two_diff` 两个用例期望值写错（该题有多解，正解是按顺序扫到的 [1,2]），用参考答案跑判分器才暴露 → 修正后期望值全绿 18/18 | `样例代码库/hash_pattern_自测.py`（9049 bytes，交付状态=4 题待写）；改动路线新增 W-20（判分器交付前必须先用正解跑一遍） |
| 2026-09-21 | 周一 | **RAG 从"看懂"到"跑通+可量"（方向 3 第一次真跑模型）**：bge-m3 下载完成后① 写 `rag/01_embedding_检索.py`：真 embedding 检索（1024 维，8 条文档现算 4.49s=562ms/条，向量缓存；问题向量化首调 2.2s 冷加载→之后 16–39ms）。实测**6/6 问题 top-1 都是四君子汤**（含字面 0 命中的「免疫」「免疫力差」「体虚乏力」「增强体质」）② 写 `rag/02_rag_完整流程.py`：检索+生成（qwen2.5:3b）。**受控实验抓出真问题**：我最初的严格提示词导致"检索对了模型却说资料里没有"→ A/B/C 三版对照后改成"鼓励用资料+保留不编底线"，四项全过（含「能治癌症吗」→ 正确答"资料里没有提到"不编造）③ 写 `rag/03_评估检索质量.py`：15 条测试集 + Recall@k/MRR/命中率 → **Recall@1=87%、Recall@3=100%、MRR=0.933**，并列出 2 个失败案例（怕冷又出汗→白虎汤盖过桂枝汤；发高烧口渴→银翘散盖过白虎汤）作为下一步改进基线 | `rag/01_embedding_检索.py`、`rag/02_rag_完整流程.py`、`rag/03_评估检索质量.py`、`rag/_run-01.txt`、`rag/_run-02b.txt`、`rag/_run-03.txt`、`rag/_embeddings-cache.json`；改动路线新增 W-19（提示词过防御导致 RAG 装死） |
| 2026-09-21 | 周一 | **方向 3（AI 应用 / RAG）纳入，并完成起步**：学员明确"选项 3 也是方向" → 三线并进。① 先探本机：**Ollama 0.34.2 已装且在跑**，已有 `qwen2.5:3b`(LLM) / `llama3.2-vision:11b` / `minicpm-v:8b`，**缺 embedding 模型**（已后台拉 `bge-m3`）② 用学员真库实测"字面匹配"的两个盲区（搜「免疫」「炎症」0 首；搜「热」过度命中 6 首）③ 实测证伪"零依赖伪向量"方案（字 n-gram 对「免疫/体虚乏力/增强体质」全 0 命中 → **语义检索必须上模型**，记 W-18）④ 产出 RAG 起步卡：三档检索（逐条扫描/倒排索引/向量）并排对比 + RAG 全流程"你在哪一步" + "模型只看到你塞给它的"心智模型 | `交互演示/RAG①-检索三档对比.html`、`_shot-rag1.png`；看板 2.1 新增「AI 应用 / RAG」行；改动路线 W-17/W-18 |
| 2026-09-21 | 周一 | **学员划边界 + 转向真主线（1+2）**：① 学员明确"Python 内置函数不用教学" → 关闭 T-04/T-05，写入路线文档第 0 条边界 ② **算法 150 题起步**（阶段 A 主目标）：新增哈希表模式卡（O(n²) vs O(n) 并排逐步实测，10 个数时 45 次 vs 10 次；去重/无解边界；8 处常见错；同模式 5 个变式）+ `样例代码库/two_sum.py`（真实跑分：n=20000 时暴力 12.9s vs 哈希 0.0035s，3712×）③ **pytest 收尾达成**：新建 `conftest.py`（共享缓存内存库 fixture，含"每次请求新连接"与"跨线程"两个坑的解法）+ `test_api_v2.py`（21 个测试，fixture + 参数化，覆盖全部 9 接口）→ 全套 **27 passed / 0.25s**，真库仍 8/33/49 无污染 | `交互演示/算法①-哈希表查找.html`、`_shot-algo1.png`、`样例代码库/two_sum.py`、`样例代码库/_run-two_sum.txt`、`conftest.py`、`test_api_v2.py`；看板 2.1（pytest ✅ / 算法 🔄）；改动路线新增 W-16 |
| 2026-09-21 | 周一 | **函数详解卡补齐 + 第 4 课开工**（学员：SQL 基础已完成，其他函数也要抽丝剥茧式，继续下一步）：① `range`/`enumerate` 重做成详解版（与 `len` 同构：签名 → 参数类型 → 返回类型 → 异常 → 边界 → 自己写）② 新增**可检索语法速查**（39 个内置函数，含"坑"一列）③ 新增**函数与参数**主课卡（用学员自己 `prescription_db.py` 的 `link()` 讲：为什么需要函数 / 签名解剖 / 4 种参数 / 7 种调用绑定演示 / return vs print / 作用域）④ 写 `第4课-函数与参数.md` 教案（20 分钟微课 + 三级验收）⑤ 8 张卡全量 `node --check` 语法体检通过 | `交互演示/range卡片-详解.html`、`enumerate卡片-详解.html`、`语法速查-常用内置函数.html`、`函数与参数-详解.html`、`_shot-range/enum/bifs/func.png`、`第4课-函数与参数.md`；改动路线补坑 W-14（揭晓按钮先 render 再读输入 → 永远"先填一个数"）、W-15（速查卡单引号嵌套致整页 JS 崩） |
| 2026-09-20 | 周日 | 应约读通工作区全部文档 → 备 **第 3 课（数据模型 + JOIN）**：诊断 P-009 断点（假设=关系模型而非语法）、设计"三步推导法"教案、换皮到他自己建过的 `prescriptions.db`；实测教案里每条 SQL，**证伪并修正了自己初稿的一个错断言**（"ON 写错必然空结果"——id 空间重叠时实为返回伪正确数据） | `第3课-数据模型与JOIN.md`、`第3课-微信群演示.py`、`教案SQL校验-20260920.txt`、`JOIN诊断-校验-20260920.txt`、`演示脚本-实跑输出-20260920.txt`；看板表 2.2/2.3/3.2/5.1/附录C 已更新 |

**状态备注**：第 3 课**尚未实际上课**，看板记 📋「教案就绪，待上课」；P-009 由 🔴 转 🔄（处理中）。

**实测数据修正（供后续引用）**：`prescriptions.db` 实测 **8 首方剂 / 33 味药材 / 49 条关联**；
`sql-notes.md` 第八节记的是"8 / 37 / 48"，与库不符 —— 未擅自改他的笔记，留作第 3 课让他自己 `COUNT(*)` 数一遍（检索练习）。

| 2026-09-20 | 周日 | **卡片加厚 + SQL 交互三连**（学员反馈"交互内容解释太少，要参数种类/输出类型种类；SQL 也要同样详解"）：① `len` 卡重做为详解版（签名 → 参数种类分"能数/不能数" → 交互 → 返回类型 → 4 种用法 → 5 类异常报错 → 边界 → 自己写）② SQL 新增三张分层详解卡：查询与筛选（语句骨架/NULL 坑/排序/限制/**带真模拟器**：改 SQL 真跑 + 五步执行拆解）、分组与聚合（五个聚合函数签名/COUNT(*) vs COUNT(列)/GROUP BY 分堆过程/WHERE vs HAVING/执行顺序图）、JOIN 详解（为什么拆表/三步推导/四种连接/ON vs WHERE/NULL 陷阱/错配诊断）③ 全部用 Playwright 真点验证（①8 项 ②8 项 ③5 项 ④14 按钮） | `交互演示/SQL基础①②③-*.html`、重写的 `len卡片-最小范例.html`、`_shot-SQL①/②/③.png`、`分组统计-校验.txt`；`改动路线.md` 补坑 W-11…W-13 |
| 2026-09-20 | 周日 | **教学法升级 v3（交互式 · 微课化 · 可打印）** —— 学员反馈三件事：白天有学校课约不上课 → 一直拖；陌生函数忘用法 → 进度慢；知识要可视化可调参。据此产出三件武器：① 交互演示 HTML（第一张：JOIN 配对过程，能拧参数、逐步看配对）② 样例代码库（len/range/enumerate/sql_join 四段式模板）③ 可打印 PDF（逐日计划/速查卡/记忆曲线打卡 + Edge 无头打印流水线）；另建 `改动路线.md` 防 compact 失忆 | `teaching-methodology-v3-交互式微课.md`、`交互演示/JOIN配对过程.html`、`样例代码库/*.py`(4)、`打印资料/*.html`+`*.pdf`(3+3)、`tools/html2pdf.py`、`改动路线.md` |
