# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**唯一使用者：韩立（刁健宇），19 岁，安徽中医药大学 计算机科学与技术（讯飞班）25 级，2029.06 毕业，目标岗位 AI 应用工程师。**

他的真实使用场景（`[实测]` 来自本工作区账本与教学法的历次记录）：

- **碎片化、不固定**：白天有学校课，约不上整块时间；典型一次 30–60 分钟，常常在晚上或宿舍。
- **一个人学，没人陪**：教练（老师）只能在特定时刻出现，中间大段时间是自学。
- **会忘**：自述「陌生函数忘用法，进度慢」；2026-09-23 实测 09-06 学的 404 三步排查法 17 天后已完全想不起来。
- **要给别人看**：导师、未来的面试官 —— 部分材料需要**打印成 PDF** 或**发出去**。

## Product Purpose

**把"老师当面讲一遍"这件事，固化成可以反复打开、自己拧、自己测的交互式网页**，让学习不再依赖"老师此刻在不在"。

一次成功的产物长这样：韩立打开一个 HTML，在**没人讲解**的情况下，通过自己动手拧参数、看过程、猜了再验证，把当次课的概念真的学会；并且在**几周后**再打开时，能一眼看到"我学到哪了、下一步是什么"。

## Positioning

不同于任何在线课程或题库：**这些页面是为他一个人的真实进度、真实错误、真实数据手工做的。**

- 用的数据是**他自己建的库**（`prescriptions.db`，8 方 / 33 药 / 49 关联）和**本机实测输出**，不是编的示例。
- 页面里写的坑，是**他在本工作区真实踩过的坑**（记在 `改动路线.md` 的 W 表里）。
- 判断"要不要做成卡片"有一条**本地规则**：参数多 / 行为随参数变 / 过程状态变化 → 做卡片；"就是名字记不住" → 只做速查（见 `_卡片模板.html` 的「3 秒判断」）。任何一门课都不会有这条规则。

## Operating Context

- **文件都在 `E:\项目学习\交互演示\`，双击用浏览器打开**，没有服务器、没有构建步骤、**不联网**（本机离线也要能用）。
- 同伴产物：`打印资料/`（A4 PDF，Edge 无头打印，见 `tools/html2pdf.py`）、`样例代码库/`（同名 `.py`）、`教学进度看板.md`（唯一权威的进度表）。
- **权威事实来源**：`learning-roadmap.md`（v4 就业优先路线 · 倒推链 · 阶段 A–F）、`教学进度看板.md`（五表制度）、`改动路线.md`（T 表待办 / W 表踩坑 / 教学法）、`每周目标.md`（滚动周目标）。
- **教学法 v3**（`teaching-methodology-v3-交互式微课.md`）规定每张卡的核心结构：**先猜 → 能拧的参数 → 并排看变化 → 看得见的过程 → 能复制的真代码 → 你先自己写**。
- ⚠️ **教学红线**：中医数据只当作业载体，**禁止讲中医语义**。

## Capabilities and Constraints

**必须做到**

- 单文件、离线、双击即开；手机浏览器（约 390px 宽）也要能读。
- 参数一改，结果**立即**变，不需要点"运行"。
- 进度、勾选状态等要能**记住**（localStorage）。
- 每张卡必须能回答"**我现在在全局的哪一格**"。
- 部分卡要能被 `tools/html2pdf.py` 打成 A4，所以打印时不能丢内容。

**已知约束**

- **纯静态 HTML/CSS/JS**，不引入框架、不引入 npm 依赖（现有 13 张卡全是自包含单文件）。
- 不允许为了效果去联网拉字体/CDN。
- 中文排版为主，正文可读性优先于装饰。

**明确未决**

- 是否把现有 13 张暗色卡统一换皮 —— 2026-09-23 用户选择「**先做底层+模板+全局思维导图，现有卡片不动**」。

## Brand Commitments

用户 2026-09-23 明确指定的、**不可协商**的视觉约定：

1. **护眼方格纸** —— 底色用**米黄**（当次确认：三种候选里只要米黄），带方格网格。
2. **抽象的东西要加动画**。
3. **每份课前 HTML 都要有思维导图**；另有**一份全局思维导图**，标出"当前学习处于全局哪个阶段"。

## Evidence on Hand

- **真实数据**：`prescriptions.db`（自建，8/33/49）；`第3课-实跑输出-20260923.txt`（本机真实执行输出）。
- **真实踩坑**：`改动路线.md` W-01…W-36。
- **真实进度**：`教学进度看板.md` 表 2.1（各技术栈状态）、表 5（P 表问题记录）。
- **不许编造**：任何"他掌握了 X"的断言必须有看板或账本依据；页面里的命令输出必须来自本机实测。

## Product Principles

1. **先猜再给答案。** 生成效应优先于讲解；页面必须留"你先答"的位置。
2. **看得见过程，而不只是结果。** 中间步骤要高亮、能单步播放。
3. **数据必须是真的。** 宁可少做一张卡，也不用编造的示例输出。
4. **永远回答"我在哪"。** 每张卡都要能定位到全局路线的某一格。
5. **不打断、不表演。** 打开就能读，动效服务于理解，不服务于炫技。
