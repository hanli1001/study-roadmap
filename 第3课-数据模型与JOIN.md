# 第 3 课 · 数据模型与 JOIN（教学卡片）

> 配套：`teaching-methodology.md`（教学法 v2）· `教学进度看板.md` · 学员自己的 `prescription_db.py` / `sql-notes.md`
> 时长：**45 分钟（碎片化适配，可断成 2 次：①–④ / ⑤–⑥）** · 前置：第 1、2 课已毕业
> 专攻：**P-009 ——「JOIN 关系表一直不理解」（🔴）**

---

## 本课要打的靶子：不是语法，是"绳子"

P-009 的字面是"JOIN 不理解"，但 `sql-notes.md`（2026.07 他自己写的）里**多对多、关联表、JOIN 语句、执行过程四节全都在**。
→ 所以本课的假设是：**语法是会的，断点在"关系模型"**（为什么非要第三张表 / 执行时发生了什么）。开课先用 8 分钟验证这个假设，别急着讲。

**本课最多 3 个新概念**（认知负荷红线）：
1. **第三张表是什么角色**（绳子：只记"谁连着谁"）
2. **`ON` 是配对规则，不是筛选条件**（`WHERE` 才是筛选）
3. **JOIN 的执行过程**：一行一行带着 id 去另一张表问名字

> ⚠️ 教学红线：**不讲解中医语义**（不问"这方治什么病"）。方剂数据只当"你已经建过的库"来用。
> 素材：微信群（推导）→ 选修课（变式）→ 自己的 `prescriptions.db`（换皮落地）。

---

## ① 闭卷快问快答（5 分钟）—— 第 1、2 课检索

不翻笔记，答不出就给提示、别批评（检索练习 + 失败正常化）：

1. 第 1 课：`=` 和 `==` 有什么区别？→（一个等号搬东西，两个等号问问题）
2. 第 2 课：浏览器报 404，你的三步排查法是？→（/docs 看菜单 → 对路径 → 对端口）
3. 第 2 课：`conn.close()` 忘了写，类比里叫什么？→（用完的托盘要放回）
4. 预热：`sql-notes.md` 第八节那个库里，一共几张表？→（3 张）

---

## ② 钩子 + 先猜（3 分钟）：一个"做不到"的查询

给他看这张表（**只有数字，没有名字**）：

```
群成员表 group_members
+-------+-----------+------------+
| gid   | uid       | joined     |
+-------+-----------+------------+
| 1     | 7         | 2026-03-01 |
| 1     | 9         | 2026-03-02 |
| 2     | 7         | 2026-04-11 |
+-------+-----------+------------+
```

**先猜，不许翻笔记**：
> "我要把 1 号群**成员的名字**列出来，一条 SQL 写出来 —— 你先猜：这条 SQL 需要用到几张表？"

- 猜 **1 张** → 断点是 (A) 关系模型：他没意识到姓名不在这张表里
- 猜 **2 张** 但写不出 → 断点是 (B) 执行过程
- 写了 `WHERE`、`ON` 混着放 → 断点是 (C) 配对 vs 筛选

**留白 30 秒**，让他自己撞墙再讲（回忆失败也有用，叫测试效应）。**答案先不给**。

---

## ③ 画图推导（8 分钟）：三张表是怎么"逼"出来的

**话术**：不背规则，我们从一个微信群的需求，一步步把表**逼**出来。

### 三个业务事实

1. 一个人 → 一个手机号（一个格子就够）
2. 一个群 → 一个群名
3. **"谁在哪个群 + 什么时候进的"** → 这格子该放哪？

### 三步推导法（本课核心，写在纸上）

| 步骤 | 问自己 | 微信群例 |
|------|--------|----------|
| ① 数一数，有几种"东西"？ | 每样东西一张表 | 人 / 群 → 2 张表 |
| ② 每个事实**跟着谁走**？ | 只跟一个人走 → 放人表；只跟一个群走 → 放群表 | 手机号跟人走；群名跟群走 |
| ③ 跟**两个人**走的事实？ | 这就是**绳子** → 单独一张表 | "谁在哪个群" → **第三张表** |

### 建表（第 1 张 + 第 2 张，他已经在 `prescription_db.py` 里写过同构的）

```sql
CREATE TABLE persons (
    id    INTEGER PRIMARY KEY AUTOINCREMENT,
    name  TEXT NOT NULL,
    phone TEXT
);

CREATE TABLE groups (
    id   INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);
```

### 第 3 张表：先猜再写（生成效应）

> "现在第 3 张表要记'谁在哪个群、什么时候进的'。**先猜**：它需要哪几列？"

```sql
CREATE TABLE group_members (
    uid    INTEGER,                 -- 指向 persons.id
    gid    INTEGER,                 -- 指向 groups.id
    joined TEXT,                    -- 加入时间
    FOREIGN KEY (uid) REFERENCES persons(id),
    FOREIGN KEY (gid) REFERENCES groups(id)
);
```

**逐行问**（对照他已经写过的 `prescription_ingredients`）：
- 为什么 `joined` 不能放 `persons` 表？→ "一个人加了 10 个群，难道存 10 行？"
- 为什么不能放 `groups` 表？→ "群有 200 人，群名要存 200 遍？"
- 这两列数字是**名字**吗？→ 不是，是**绳子**（id）

### 故意填错（关键的"为什么"环节）

> "一个群 200 人，但我不知道谁知道谁是管理员 —— **`role`（群主/成员）这一列放哪？**"

- 放 `groups` → 群有两个管理员就存不下（一格只能装一个值）
- 放 `persons` → 他在 A 群是管理员、在 B 群是普通成员 → 装不下
- **放 `group_members`** → 每条绳子各自带一个身份 ✅（跟 `dosage` 一模一样的位置）

### 画图收口（让他自己画，别给答案）

```
persons          group_members            groups
+----+------+    +-----+-----+--------+   +----+-----------+
| id | name |    | uid | gid | joined |   | id | name      |
+----+------+    +-----+-----+--------+   +----+-----------+
| 7  | 小明 |<---| 7   | 1   | 03-01  |-->| 1  | 高数自习  |
| 9  | 小红 |<---| 9   | 1   | 03-02  |   | 2  | 游戏开黑  |
| 7  | 小明 |<---| 7   | 2   | 04-11  |   +----+-----------+
+----+------+    +-----+-----+--------+
     ↑ 名字在这边            ↑ 只有数字：绳子
                 ↑ 中间这张 = 绳子，两边各拉一头
```

> 一句话：**第三张表不是"数据表"，是"绳子表"；它的行 = 一条关系，不是一个人。**

---

## ④ 推导 JOIN（8 分钟）：一行一行，带着 id 去问名字

### 先猜后跑（生成效应）

先给出完整示例（可运行，10 行内）：

```sql
SELECT persons.name, groups.name
FROM groups
JOIN group_members ON groups.id = group_members.gid
JOIN persons       ON group_members.uid = persons.id
WHERE groups.id = 1;
```

**逐行问**（方法 v2 示例-问题对）：
- `FROM groups` —— 先站哪张表？（主干：我要 1 号群）
- 第 1 行 `ON`：拿什么去配什么？（`groups.id = group_members.gid`）
- 第 2 行 `ON`：绳子拿到了 `uid`，去哪换名字？（`persons` 表）
- `WHERE groups.id = 1` —— **为什么这条写在最后、长得还不一样？**（它才是"筛选"）

### 执行过程（本课最该讲透的一段，用表格演）

| 步 | 数据库在干什么 | 数据长什么样 |
|----|----------------|--------------|
| 1 | 从 `groups` 拿出 1 行：`id=1, 高数自习` | `1 | 高数自习` |
| 2 | 拿着 `1` 去 `group_members.gid` **一行行比** | 命中 `(7,1)` ✓ `(9,1)` ✓ `(7,2)` ✗ |
| 3 | 每条命中的行，拿 `uid` 去 `persons.id` 换名字 | `7→小明`、`9→小红` |
| 4 | 拼成一行交出去 | `小明 | 高数自习`、`小红 | 高数自习` |

**点破两个"原来如此"**：
- 结果有 **2 行**，因为 1 号群有 2 个成员 —— **行数不是表里的行数，是"配对成功"的次数**
- 如果 `ON` 写错（比如写成 `groups.id = group_members.uid`）→ 一行都配不上 → **0 行**，而且**不报错**
  > 👉 所以 `JOIN` 查出来是空的时候：先怀疑 `ON`，别怀疑数据没了

**⚠️ 但"0 行"不是唯一后果（本课实测过的两种情况，见 `第3课-微信群演示.py`）**：

| 情况 | 实测结果 | 危险度 |
|------|----------|--------|
| id 空间**不重叠**（微信群例：人 7/9、群 1/2） | 错配 → **0 行** | 低：空的，一眼看得出来 |
| id 空间**重叠**（两套都从 1 开始编号、id 密集） | 错配 → **照样出数据，而且看着挺合理** | 🔴 **高：静默错误** |

> 实测反例：本工作区 `prescriptions.db` 里 `prescriptions.id` 与 `ingredients.id` **都从 1 开始**。
> 把配对准写成 `p.id = pi.ingredient_id` 后，查询**返回了 20 行**（麻黄汤配麻黄…），
> 看着像对、其实全错，而且**不报错**。
> 👉 结论：`JOIN` 要核对的从来不是语法，而是**配对对不对**；"不为空"绝不等于"对"。

### 立即复刻（换皮第 1 跳：从中文伪代码到真 SQL）

```sql
-- 练习题：把「1 号群的成员名字」改成「2 号群」
SELECT persons.name FROM ??? JOIN ??? ON ???  WHERE ???;
```

---

## ⑤ 换皮 + 迁移（15 分钟）：搬回你自己的库

**说明**：下面用的就是你 7 月自己建的 `prescriptions.db` 三张表（`prescriptions` / `ingredients` / `prescription_ingredients`）。
**结构一模一样，只是绳子换了名字** —— 这就是"迁移"，不是"新东西"。

### 对照表（让他自己连）

| 微信群例 | 你的方剂库 |
|----------|-----------|
| `persons`（人） | `ingredients`（药材） |
| `groups`（群） | `prescriptions`（方剂） |
| `group_members`（谁在哪个群） | `prescription_ingredients`（哪方有哪药） |
| `joined`（加入时间） | `dosage`（用量） |

### 换皮 1（L1 复刻 · 照抄改名）

```sql
SELECT p.name, i.name, pi.dosage
FROM prescriptions p
JOIN prescription_ingredients pi ON p.id = pi.prescription_id
JOIN ingredients i ON pi.ingredient_id = i.id;
```

> 跑之前**先猜**：会出多少行？（实测 **49 行** —— 因为关联表里正好 49 条绳子）

### 换皮 2（L2 变式 · 反着查，不从方剂出发）

> "反过来：**甘草**出现在哪些方剂里？" —— 起点要换成哪张表？

```sql
SELECT p.name, pi.dosage
FROM ingredients i
JOIN prescription_ingredients pi ON i.id = pi.ingredient_id
JOIN prescriptions p ON pi.prescription_id = p.id
WHERE i.name = '甘草';
```

### 换皮 3（L2 变式 · 统计，见识 `LEFT JOIN` + `GROUP BY`）

```sql
SELECT p.name, COUNT(pi.ingredient_id)
FROM prescriptions p
LEFT JOIN prescription_ingredients pi ON p.id = pi.prescription_id
GROUP BY p.id;
```

> 只讲一句：`LEFT JOIN` = "左边的表**一首都不能丢**，配不上的填 NULL"。
> 点破：如果把 `LEFT` 去掉，**药材数 0 的方剂会整行消失**（这是统计时最常见的坑）。

---

## ⑥ 自我解释（5 分钟）：他说，我听

不写代码，**让他用自己的话讲**（讲不出 = 还没会，方法 v2 第八章）：

1. `ON` 和 `WHERE` 有什么区别？（配对 vs 筛选）
2. 为什么关联表里**存数字 id，不直接存名字**？（名字会变 / 会重复 / 占地方）
3. `dosage` 为什么放关联表，不放方剂表也不放药材表？（同一个"绳子"位置）
4. 中途插一句："`JOIN` 查出来是空的，你先怀疑什么？"（怀疑 `ON`）
5. 追问："那查出来**不为空**，就说明配对一定对吗？"（**不一定** —— 见④的实测反例）

**顺手给一个可背的诊断三步**（这是本课最实用的迁移技能）：
1. **单独**数绳子表：`SELECT * FROM prescription_ingredients;`（实测 **49** 行）
2. 再数 JOIN 的结果行数（也是 **49** 行）
3. 两数一致 **≠** 配对正确 —— 还要**挑一行人工核对内容**（id 重叠时会"数字对、内容错"）
4. 数字明显对不上（空 / 多很多）→ 几乎一定是 `ON` 写错

---

## ⑦ 闭卷小测（3 题）+ 复习卡（5 分钟）

**检索小测（合上所有笔记）**：
1. 多对多关系为什么必须用第三张表？（用自己的话，一句）
2. 这条 SQL 会出几行？为什么？
   ```sql
   SELECT persons.name FROM persons
   JOIN group_members ON persons.id = group_members.uid
   WHERE group_members.gid = 1;
   ```
3. 找错：`JOIN group_members ON groups.id = group_members.uid` 错在哪？
   → 群 id 去对人 id（绳子两头接反）：**可能 0 行，也可能给你"看着合理"的假数据**，都不报错

**三张复习卡（进 1-3-7-30 日程）**：
```
【卡片 1 · 概念】问：什么是"绳子表"（关联表）？为什么多对多必须用它？
【卡片 2 · 预测】问：SELECT p.name, i.name FROM prescriptions p JOIN prescription_ingredients pi
                ON p.id=pi.prescription_id JOIN ingredients i ON pi.ingredient_id=i.id;
                会出几行？（答：49 行 = 绳子数）
【卡片 3 · 找错】问：JOIN ... ON groups.id = group_members.uid 错在哪？
                （答：群 id 对人 id，两头接反；① id 不重叠 → 0 行、不报错
                  ② id 重叠 → 返回"看着合理"的假数据，同样不报错）
```

**课后任务（L1→L2，15 分钟）**：
> 仿照③的三步推导法，自己设计一个**选修课报名**的关系图（学生 / 课程 / 选课），
> 三张 `CREATE TABLE` + 插 3 行数据 + **一条 JOIN 查询**跑通。

---

## 验收标准（三级 · 方法 v2 第八章）

| 级别 | 通过标准 |
|------|----------|
| **L1** | 能不看笔记口述③的三步推导法，并说出 JOIN 的执行过程（一行行带 id 去换名字） |
| **L2** | 30 分钟内独立建出选修课三张表 + 写出 JOIN 查询并跑通 |
| **L3** | 能讲给别人听："为什么多对多必须要第三张表" + "不存 id 直接存名字会怎样" |

> ⚠️ 学员说"我懂了"→ **危险信号**，立刻用上面第 2 题的检索题验证（看懂 ≠ 学会）。

---

## 导师自查清单（课前过一遍）

- [ ] ②的"先猜"有没有真的让他猜？（别直接给答案 —— 生成效应）
- [ ] 新概念数 ≤3？（第三张表 / `ON` 配对 / 执行过程；`LEFT JOIN` 只是见识）
- [ ] 全程**没有讲中医语义**？（方剂只当"你已经建过的库"）
- [ ] ⑤的对照表是**他自己连**的，不是我念的？
- [ ] 有没有让他**亲眼看一次 `ON` 写错的两种后果**（空 / 伪正确）？→ 跑 `第3课-微信群演示.py` 演示 3、4
- [ ] 课后 3 张复习卡 + 选修课任务发出去了吗？课后 30 分钟内更新看板 + git 提交？

---

## 附：配套文件（都在本工作区）

| 文件 | 用途 |
|------|------|
| `第3课-微信群演示.py` | 课堂现场跑：4 个演示（绳子表 / 正确 JOIN / 错配空结果 / 错配伪正确数据） |
| `演示脚本-实跑输出-20260920.txt` | 上面脚本的实测输出（课上可直接对照，不用现场赌） |
| `教案SQL校验-20260920.txt` | 教案里每条 SQL 对着 `prescriptions.db` 的实测结果（8/33/49 等数字出处） |
| `JOIN诊断-校验-20260920.txt` | "错配到底空不空"的对照实验记录 |

## 附：下次课接口

- 本课若 L2 通过 → 第 4 课（pytest 进阶）可直接用"测试 JOIN 查询结果"当素材（交错练习）
- 遗留观察点：**`sql-notes.md` 第八节数字与库不一致**（笔记记 37 味药材 / 48 条关联，实测 33 味 / 49 条）
  → 上课时顺带让他自己 `SELECT COUNT(*)` 数一遍（**检索练习 + 让他开始不轻信笔记数字**）
  → ⚠️ 我**没有改**他的笔记数字，先存疑；下次课让他自己数完再定，避免我单方面覆盖他的学习记录

---

*第 3 课卡片 v1 · 2026.09.20 · 依据：教学法 v2（示例-问题对/生成效应/检索练习/认知负荷）+ P-009 诊断设计 · 数据实测自 `prescriptions.db`*
*v1.1（同日）：教案初稿中"ON 写错必然空结果"经实测证伪（方剂库 id 重叠时返回 20 行伪正确数据）→ 已按两种真实后果改写，并由 `第3课-微信群演示.py` 复现*
