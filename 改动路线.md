# 改动路线 · 决策与坑（防失忆文档）

> **这份文件是给"未来的我"看的。** 会话一 compact、上下文一丢，路线和踩过的坑就会重演。
> 规则：① 每次改教学体系/新增文件体系 → 补一条 ② 每个坑写完必须带"怎么修的" ③ 待办写清"卡在谁身上"
> 学员：韩立（用户本人）· 导师：AI · 最后更新：2026-09-20（周日）

---

## 〇、⚠️ 这个学员一共有几条线（**写任何"全局路线 / 时间预算"之前，先读这里**）

> **这条是 W-33 的结构性修复。** 2026-09-22 我做路线 v4 时**漏掉了整条 `E:\物联网`（导师任务）** ——
> 根因不是判断错，是**我根本不知道有几条线**。所以把索引搬进本文件（本文件是"compact 后先读"的那份），
> **读到这里就一定会看到全部线**，不再靠我记性。
>
> 完整索引（15+ 个工作区）：`E:\dsh优化\时间线账本.md` 第 50–70 行。下表只列**会抢时间的**。

| 线 | 主目录 | 自己的账本 | 谁在推 | 进 2026 秋预算？ |
|----|--------|-----------|--------|:----------------:|
| **教学线** | `E:\项目学习` | 同目录 `时间线账本.md` | 教练（我） | ✅ 就是本工作区 |
| **🔵 物联网 / 毕设线** | `E:\物联网` | ✅ 有 | **导师**（有真人在等） | ✅ **本周主轴** |
| **英语线** | `E:\学习\英语` | ✅ 有 | 自己（死线 **2026-12-12** 四级二战） | ✅ 45 min/天，**独立预算** |
| **学校专业课** | —（非工作区） | — | 学校 | ✅ **不可压缩**（导师本人要求优先） |
| 硬件赛 | `E:\硬件赛` | ✅ 有 | 竞赛 | ❓ **待确认**（总账记"竞赛死线在那边"） |
| 学校机器学习 | `E:\学校机器学习` | ✅ 有 | 课程 | ❓ **待确认**（实验作业） |

**⇒ 2026 秋的时间口径**：技术侧（教学 / 物联网 / 求职作品）共用 **10.5–14 h/周**；英语 **5.25 h/周**另计；学校课不可压缩。
**⇒ 排量纪律**：同一周只保**一条主轴**，判据 = **谁的"停一天有人问"**（详见 `learning-roadmap.md`「🔵 第二条线 · 容量账」）。

> ⚠️ **另有约 9 个工作区状态未查**（`E:\VR` / `E:\小爱音响` / `E:\电脑优化` / `E:\讯飞项目` /
> `E:\CCProjects\思想延申` / `E:\计算机设计` / `E:\学习资源整理` / `E:\国创赛` / `E:\自我提升\健身`）
> —— `[未验证]`，**不确定它们是否还在占时间**。排"全局"计划前先问学员一句。

> 🔀 **2026-09-22 已决：物联网线「只在计划层打通，物理不合并」** ——
> 两个工作区各自独立、各有账本，靠本文与 `learning-roadmap.md`「🔵 第二条线」互相引用。
> **理由**：`E:\项目学习` 连着**公开** GitHub 仓库，而 `E:\物联网` 里有**别人的毕业论文 + 导师的科研材料**（详见 W-35）。
> 分开 = 敏感件在仓库外，风险自然为零。

---

## 一、现在的文件体系（去哪找东西）

```
E:\项目学习\
├── 教学进度看板.md ................ 唯一权威：学生情况/进度/方案/职业/问题五表
├── teaching-methodology.md ........ 教学法 v2（学习科学原理，仍有效）
├── teaching-methodology-v3-交互式微课.md ... 教学法 v3（交互/微课/可打印，2026.09.20 新增）
├── 改动路线.md .................... ★ 本文件
├── learning-roadmap.md ............ 学习路线图 v4（**就业优先**，含倒推链条 + 阶段 A–F）
├── 每周目标.md .................... ★ **本周要做什么**（每周一重写 · 滚动文件 · 永远只有当前周）
├── 周目标存档/ .................... 历周目标归档 + `_模板.md`
│
├── 第0/1/3课-*.md ................. 教学卡片（Markdown，可 git diff、可复制去跑）
├── 交互演示/*.html ................ ★ 交互式卡片（单文件、离线、手机可开、能拧参数）
├── 样例代码库/*.py ................ ★ 每学一个函数/算法 → 一个能跑的 demo
├── 打印资料/*.html + *.pdf ........ ★ 可打印资料（PDF 由 tools/html2pdf.py 生成）
├── tools/html2pdf.py .............. 打印流水线（Edge headless，不需要装库）
│
├── *-notes.md ..................... 学员自己的笔记（Git/SQL/FastAPI/Docker）
├── prescription_db.py / api.py .... 学员自己的项目代码
└── prescriptions.db ............... 学员的 SQLite 库（8 方剂 / 33 药材 / 49 关联，实测）
```

**三种产物的分工（2026.09.20 定的，别再混）**：

| 用途 | 用什么 | 为什么 |
|------|--------|--------|
| 讲"抽象/过程/参数变化" | **HTML 交互** | 能拧参数、看得见中间步骤 |
| 要复制去跑、要进 git、要 diff | **Markdown** | 文本可复制可版本管理 |
| **这周干什么**（每周一换） | **`每周目标.md`**（Markdown 滚动） | 一个链接永远有效；归档进 git 可回看"当时到底做了什么" |
| 要打印、要每天打勾 | **PDF**（源是 HTML） | 打印件必需，手机看也行 |

---

## 二、体系变更记录（倒序）

| 日期 | 变更 | 触发原因 | 涉及文件 |
|------|------|----------|----------|
| **2026-09-22（三补）** | ✅ **容量口径澄清 → 撤回我自己一条过重的警告**：学员答「1.5–2 小时/天**不含**英语的 45 分钟」→ 技术三线可用 **10.5–14 h/周**（英语 5.25 h/周另计，独立线）。**W39 需要 7.75–10.25 h ⇒ 够用。** 上一版我按「已含英语」的保守假设写下「技术线只剩 5–9 h/周 → **并行可能直接崩**」—— **该结论不成立，已撤回**（原始假设与结论在 `learning-roadmap.md` 原处保留对照，不删，便于以后看清哪一步错了）。纪律相应调整：① **按 1.5 h/天（下限）排量，把 2 h/天当奖励** ② 余量不填满，留意外缓冲 ③ **「主轴错峰」的定位从"生存必需"下调为"效率手段"**（账够；错峰是为了减少上下文切换成本）。另新增一条 **「有余力才做」**：物联网线的「**理清楚**」—— 导师点名要求做且**不依赖他的回复** | 学员答 T-20 | `教学进度看板.md`（1.3 口径定稿）、`learning-roadmap.md`（容量账定稿 + 撤回记录）、`每周目标.md`（预算重算 + 撤回警告 + 有余力项）、本文件（T-20 ✅） |
| **2026-09-22（再补）** | 🔑 **三个关键问题的答案到手，路线定稿**：① **导师推荐实习「只看能力，与考研无关」** → 纯就业资产，**2028.03 分岔口画法不变** ② **两条项目线选「并行」**（物联网线 = 毕设/导师线；自建 AI 应用 = 求职作品线），两条都保留 ③ 🔴 **表 1.3 空了一个月终于填上：编程主线 1.5–2 小时/天** —— 但扣掉英语线 45 min/天（自己的死线 2026-12-12）+ 学校课（导师要求优先，不可压缩），**留给技术三线只剩 ≈5–9 h/周** ⚠️*（此数按"含英语"假设算，后经学员澄清修正为 10.5–14 h/周 —— 见上方「三补」行）* ⇒ **立纪律：同一周只有一条「主轴」**，判据 = **谁的"停一天有人问"**；并立两条防翻车条款（不许同时开两个"从零开始"的动作 / 每周目标必须写明本周主轴）。**W39 排量据此重算，并给出「放弃顺序」**（第一个放弃 = 项目立项，因为它无外部截止日且可能被物联网线影响选题） | 学员答三问。**"并行"是学员选的，"容量账"是我算的** —— 他没算过这笔账，算完才好判断并行到底付不付得起 | `learning-roadmap.md`（🔀 待决 → ✅ 已决 + 容量账 + 主轴轮换表）、`每周目标.md`（⏱ 时间预算 + 放弃顺序）、`教学进度看板.md`（表 1.3 补齐 + 4.1 澄清）、本文件（T-07 ✅ / T-18 ✅ / 新增 T-20） |
| ⚠️ **上一条（"再补"）里出现的「留给技术三线只剩 ≈5–9 h/周」是当时按"含英语"保守假设算的，后经学员澄清已修正为 10.5–14 h/周** —— 见其上方「三补」行。**读历史条目时以最新口径为准。** | 口径修正留痕 | 防旧数被当现结论引用 | 本文件 |
| **2026-09-22（补）** | 🔵 **补入一整条被漏掉的工作区线：`E:\物联网`（导师任务 / 毕设候选）** —— v4 初版**只算了教学线，通篇没提它**。学员指出后核实：① 这是**导师本人的科研项目落地**（导师原话），交给他的是学长毕设《面向多模态感知的智慧养老原型系统》，任务是**完善三个算法**（服务发现 / 服务选择 / 服务组合），导师**据此推荐实习** ② 导师口径已**五次下调**，最新（09-20 第五次）为「**本科生不要求创新，用现成算法把流程实现一遍**」→ 只需 pandas / sklearn / 大模型 API / pulp / Streamlit 的**调用级**用法，**不需要深度学习 / 强化学习** ⇒ 结论由「缺 3–6 个月地基」**修正为「现有能力基本够用，现在就能开始」** ③ 最小 Demo **已搭出并实跑通过**（`E:\物联网\demo\`，退出码 0 / 5,598 字节），**但截至今天仍未给导师看过（已 2 天）** ④ 导师明确「**这套工作可以成为你自己的本科毕设**」+「**专业课优先，不得挤占**」 | 学员主动告知「物联网那个工作区也算我的一部分，是导师给的任务」—— **这是我自己漏掉的一整条线**，会把 v4 的"项目"算错（见 W-33） | `learning-roadmap.md`（新增「🔵 第二条线」+ 毕设排期 + 合并/并行待决）、`每周目标.md`（新增 🔵 独立节奏段）、`教学进度看板.md`（4.1 / 4.5）、本文件 |
| **2026-09-22** | 🎯 **路线升级 v4「就业优先版」+ 新增每周执行层**：学员明确决定「**考研当备选，找工作在前**」，并要求「**以后每周一更新，一周学习的要点集合 = 当前周要完成的学习目标**」。产出：① `learning-roadmap.md` **v4** —— 🔴 **项目前置**：阶段 A 主目标由「算法 150 题」改为「**自己写的 AI 应用 v1 上线公网**」，RAG 系统从 2027.06 提前到 **2026.10 立项 / 2027.01 上线** ② 算法 150 题降为「每天 1 题」的**地板**；Java 降为「可选」（`[实测]` 本科可投 17 条 JD 里只有 2 条要 Java）③ 🔴 **修掉 v3 一句被高估的话**：「双轨准备（2026.09–2028.03 无浪费）」逐科比对后**不成立** —— 就业线只覆盖 408 的 **2/4**，且**考研数学完全不在线上**，而那是最大的一块 ④ 明确 **2028.03 是分岔口**（秋招 2028.09–11 与考研初试 2028.12 只差 3 个月 → 实为二选一）⑤ 新增 **`每周目标.md`**（滚动，每周一重写）+ `周目标存档/_模板.md` ⑥ 新增 **倒推链条**（从 2029.06 往回排）作为整份路线的骨架 | 学员决定 + 学员要求周更机制。倒推发现：**2027.03 简历上就得有东西**，只剩 **23 周**，而 v3 的作品要 2027.06 才出现 —— **赶不上投递**。另：作者归属核查确认学员「经验」缺口的精确含义 = **没有一个从零自己做的 AI 应用** | `learning-roadmap.md`（**v4**）、**`每周目标.md`**🆕、**`周目标存档/_模板.md`**🆕、`教学进度看板.md`、`改动路线.md` |
| 2026-09-21 | **Linux 缺口确认 → 教材当天就位**：学员答「几乎不太会，只会最基本的几条命令」→ 缺口①坐实。产出 ① `Linux/任务清单-在你自己服务器上练.md` —— **3 微课 × 5 任务 = 15 个真实任务**，每课只 3 个新概念；**锚定他自己的服务器**（123.58.220.17 上的 FastAPI + 8009 端口 + Docker），不是玩具沙箱；每个任务带 **验证命令**（自判分）+ 折叠的提示/答案；**卡在第几个任务就是他的水平线** ② `打印资料/Linux速查卡-一页纸.pdf` —— 按「我要干什么」分类（找东西/看内容/接管道/看状态/管服务/读权限/卡住怎么办），7 处危险命令红标在前 | 学员自述 Linux 几乎不会；这是 3 条 JD 要求、几天能补、**唯一一个低成本高回报**的缺口 | `Linux/任务清单-在你自己服务器上练.md`、`打印资料/Linux速查卡-一页纸.html`+`.pdf` |
| 2026-09-21 | **写教材前先逐条验证命令语法**（反幻觉纪律）：把 `ls -lt` / `grep -rn --include` / `wc -l` / `tail -f` / `du -sh */ \| sort -rh` / `stat -c` / 重定向 + `grep -v` 全在本机实跑一遍确认输出符合预期，才写进教材。同时**明确标注** `ss` / `systemctl` / `journalctl` 是 Linux 专有、**Git Bash 里没有**（我无法在本机验证，标注出来让学员知道那是"待验证"而非"已确认"） | 教 Linux 最怕的就是凭记忆写命令 —— 学员照着敲报错，会直接毁掉信任 | `Linux/任务清单-在你自己服务器上练.md` |
| 2026-09-21 | **P-011 L4 验收：学员答案判为"未过关"** —— 原话「算法岗位居多，我现在会一些基础，差在经验和还有大部分没学习上」。判据：①"差在……大部分没学习上"是**循环论证**（换个说法还是它自己）②"会一些基础"太模糊 ③看完之后**明天不知道该干什么**。已用他自己的数据搭出合格版本供核对 | 教学法"我懂了是危险信号" | `职业探索/我的差距表-20260921.md` |
| 2026-09-21 | **发现分类口径会改变结论（重要）**：上一轮按【岗位名】分类 → 算法:应用 = 3:1；本轮按【**要求什么技能**】重算本科可投的 17 条 → **工程基础 41% / 算法 41% / 应用 18%**。结论：**「岗位叫什么」≠「它要你会什么」** —— 合肥满屏"大模型算法工程师"是讯飞的岗位设置，本科岗真正普遍要的是 **Python(5条)+Linux(3条)**。真正的分水岭是硕士岗独有的"造模型"技能（ZeRO/LoRA/分布式训练） | 学员答"算法岗位居多" → 意识到这个读法会吓到他且不准确 | `职业探索/我的差距表-20260921.md` |
| 2026-09-21 | **诚实指出"经验"缺口的具体所指**：核对了本工作区所有 AI 相关代码的**作者归属** —— `rag/01~03`、`agent_上下文消融.py` 都是**教练写的**，学员只是跑通看懂；学员自己写的是 `api.py`(39KB) + `test_api_v2.py`(27 passed) + 算法题解法。→ **"差在经验"的精确含义 = 没有一个"从零自己设计并做出来的 AI 应用"**（这也是唯一能替代学历的东西）。看板 2.1 里"智能体搭建：见过没做过"暂不改 | 学员自述"差在经验"，需查明是哪种经验 | `职业探索/我的差距表-20260921.md` 第三节 |
| 2026-09-21 | 🔴 **补抓全国对照后，修正上一轮一个"无数据支持的结论"**：上一轮我只有 27 条合肥数据，却写下"杭州/上海/深圳**应用岗密度高得多**"。补抓牛客全国 **484 条**后实测：① 搜「大模型」时**全国算法:应用 ≈ 6:1**，非合肥特有；上海 8.9:1、杭州 8.8:1 **比合肥更极端** ② 但换**应用向关键词**，「AI应用」校招 **173 条**、「Agent」校招 **180 条** —— 应用岗真实大量存在 ③ 城市分布 **杭州37/上海31/深圳21/北京18，合肥未进前 12**。**正确结论：不是"合肥偏算法"，是"合肥的 AI 应用岗本身就少"** | 学员「都抓取看看」→ 抓到新数据后必须回头核对旧结论，而不是让它挂着 | `职业探索/全国对照-牛客-20260921.md`、`JD-20条.md`（已加修正块） |
| 2026-09-21 | **抓到自己的测量偏差（方法论级）**：用「大模型」搜 → 满屏算法岗 → 得出"应用岗很少"。**这是我的关键词偏差**，不是市场结构。教训：**你用什么关键词搜，决定了你看到什么市场** —— 跟"只看想看的"是同一类错误，只是披了数据的外衣 | 同上（自查发现，非他人指出） | 同上 |
| 2026-09-21 | **抓取可行性完整留档**：✅ **牛客**（`np-api/u/job/square-search`，POST 表单，`query` 关键词 / `recruitType` 2实习·1校招 / `pageSize` 可到 50；记录含 `ext.requirements` **完整任职要求原文**，无需点详情页）；✅ **实习僧**（详情页明文，列表页薪资字体混淆）；⚠️ **BOSS直聘**（移动 API `m.zhipin.com/wapi/zpgeek/mobile/search/joblist.json` 可用，但抓 10 条后**指纹级封锁** `code:37 您的环境存在异常`，等 100 秒无效）；⚠️ **智联**（城市参数被忽略，不可信）；❌ **猎聘/拉勾**（跳 blank / 滑块） | 同上 | `全国对照-牛客-20260921.md` 第一、五节 |
| 2026-09-21 | **P-011 实抓 27 条真实 JD**（教练用浏览器采集，学员只管判断）：实习僧搜索页 + 详情页，**合肥 25/27**。产出三个硬数字：① **本科学历不卡的占 63%**（本科12+不限5 / 27，硕士10）② 算法层 : 应用层 ≈ 3:1（12 条 vs 4 条，**后经全国数据修正，见上行**）③ 日薪中位 120-180 元。**合肥的 AI 岗位池以科大讯飞的算法研究岗为主（11 条）** | 学员反馈「你不能帮我直接搜寻么，遇到登录问题可以让我手动登录」→ 采纳：机械活我做，判断留给他 | `职业探索/JD-20条.md`（27 条表格 + 结论）、`tools/jd_stats.py`（修了行号上界写死 20 的 bug + 修了实习薪资单位误标 K/月） |
| 2026-09-21 | **P-011 从"空计划"变成"可执行件"**：① `职业探索/JD-20条.md` —— 采集表 + 填写规则 + 搜索关键词 + L4 验收标准 ② `tools/jd_stats.py` —— 统计器：学历分布 / **经验分布（应届可投占比）** / 技能 Top15 / 薪资区间 / 城市 / 领域；**只读不写**、空白单独算、通用词如实列出不静默过滤 | 学员问"这个方向适合我么"→ 反复空想解决不了 | `职业探索/JD-20条.md`、`tools/jd_stats.py` |
| 2026-09-21 | **政策与趋势调研**（一手原文，链接逐条实测可达）：国发〔2025〕11号 · 十五五规划纲要（**140 页 PDF 下载 + 全文提取，人工智能出现 52 次**）· 教育部"人工智能+教育"行动计划 · 发改委 AI 人才研究 · 中医药数字化意见 · 教育部就业"百日冲刺"。产出 **6 条预测，每条标置信度 + "可能错在哪"**；交叉核实"2026 届毕业生 1270 万"（新华社/人民网/央视口径一致） | 学员要求"根据社会热点和国家政策抓信息做预测" | `政策与趋势-20260921.md`、`_ref/十五五规划纲要-全文.txt` |
| 2026-09-21 | **查清"职业方向"的文档欠账**：看板 4.2 在 09.06 已推翻"医疗 AI 编排师"假设，但 `learning-roadmap.md` 正文从未同步 → **7 处医学绑定残留近三周持续误导**。已全部标注作废（原文保留对照），并产出职业方向评估 | 学员：「我不一定非走医学方面，我虽然是医学校，但没有医学知识储备」 | `职业方向评估-20260921.md`、`learning-roadmap.md`（备份 `.bak-2026-09-21-去医学绑定`） |
| 2026-09-21 | **开 AI Agent 学习线**（教材：李博杰《深入理解 AI Agent》，49.5k star/Apache-2.0/10 章/109 实验）+ 本地复刻实验 1-1 的消融思想（零成本） | 学员给链接 `github.com/bojieli/ai-agent-book`，说"这个 agent 学习" | 见下行明细 |
| 2026-09-21 | 　└ 产出：① `样例代码库/agent_上下文消融.py`（本地 Agent 循环 + 5 组消融，判分由代码做）② `交互演示/Agent核心公式-上下文消融.html`（可拧开关 + 真实轨迹回放）③ `打印资料/AI-Agent学习路线图.html/.pdf`（2 页）④ `_ref/`（书的关键原文 + 浅克隆副本） | 同上 | `agent_上下文消融.py`、`_run-agent消融.txt`、`Agent核心公式-上下文消融.html`、`AI-Agent学习路线图.html/.pdf` |
| 2026-09-21 | 　└ 新增 `pytest.ini`（`norecursedirs` 排除 `_ref/`）：教材克隆里自带一堆第三方 `test_*.py`，不排除的话 `pytest -q` 会从 **27 passed 变成 10 collection errors**（见 W-30） | 收尾回归时发现 | `pytest.ini` |
| 2026-09-21 | 　└ **实测结论**：砍掉「工具定义」或「工具结果回灌」→ 20/20 掉到 0/20；**砍掉系统提示竟无影响**（20/20，两轮复现）；砍掉历史只伤两步题 | 让学员亲眼看见"上下文决定能力上限"，而非听讲 | `_run-agent消融.txt` |
| 2026-09-21 | **书里官方起点实验 1-1 在本机跑通**（零成本）：`--provider ollama --model qwen2.5:3b` → 注册 4 工具、自主调 `calculate`、答对 2000 | 验证"不买 API key 也能学这本书"，而不是嘴上说 | `_ref/_run-book-exp11.txt` |
| 2026-09-21 | **修 `tools/html2pdf.py` 两个真 bug**：① `src.as_uri()` 的编码正斜杠 URL 让 Edge 静默不产出文件（退出码仍是 0）→ 改用反斜杠 `file:///` 形式 ② 落盘轮询只等 6 秒，把已生成的大 PDF 误判为失败 → 放宽到 45 秒 + 大小稳定性确认 | 生成《AI-Agent学习路线图》PDF 时两次"失败"，排查出是工具本身的问题（影响**所有**可打印资料） | `tools/html2pdf.py` |
| 2026-09-20 | **教学法升级 v3**：微课化（10–20 分钟单元）+ 三件武器（交互卡片/可打印 PDF/样例代码库）+ L4 验收（会自己造工具） | 学员反馈：白天有学校课 → 约不上课就一直拖；忘函数用法；进度慢 | `teaching-methodology-v3-交互式微课.md` |
| 2026-09-20 | 新增 `交互演示/_卡片模板.html`（可复制模板 + 3 秒判断标准）与 `len卡片-最小范例.html`（最小可用示范） | 学员说"记不得是哪个函数，遇到了再说" → 把"遇到时怎么办"变成现成能力，支持 L4 | `交互演示/_卡片模板.html`、`交互演示/len卡片-最小范例.html` |
| 2026-09-20 | **卡片加厚（学员反馈"解释太少"）**：① `len` 卡补签名/参数种类/返回类型/异常/边界 ② SQL 新增 **三张分层详解卡**（①查询与筛选 ②分组与聚合 ③JOIN 详解），全部带"抽丝剥茧"的执行过程 + 模拟器 | 学员：交互式内容解释太少，要参数种类、输出类型种类；SQL 也要同样的详解 + 交互 | `交互演示/len卡片-最小范例.html`、`交互演示/SQL基础①-查询与筛选.html`、`交互演示/SQL基础②-分组与聚合.html`、`交互演示/SQL基础③-JOIN详解.html` |
| 2026-09-21 | **函数详解卡补齐 + 第 4 课（函数与参数）开课**：① `range`/`enumerate` 重做成详解版（签名/参数类型/返回类型/异常/边界，同 len 卡结构）② 新增 `语法速查-常用内置函数`（39 个函数可检索：签名/返回类型/坑）③ 新增 `函数与参数-详解`（用学员自己的 `link()` 讲 4 种参数 + 7 种调用绑定演示 + return/作用域）④ 教案 `第4课-函数与参数.md` | 学员：SQL 基础已完成，其他函数的解释也要抽丝剥茧式；继续下一步学习 | `交互演示/range卡片-详解.html`、`enumerate卡片-详解.html`、`语法速查-常用内置函数.html`、`函数与参数-详解.html`、`第4课-函数与参数.md` |
| 2026-09-21 | **⚠️ 学员划边界**：内置函数不再教学（"我明白"）→ T-04/T-05 关闭，本节第 0 条已固化。**转向真主线（学员选 1+2）**：① **算法 150 题起步**（阶段 A 主目标）：新增 `交互演示/算法①-哈希表查找.html`（O(n²) vs O(n) 并排实测 + 去重/无解边界 + 8 处常见错）+ `样例代码库/two_sum.py`（含真实性能跑分）② **pytest 收尾**：新增 `conftest.py`（内存库 fixture，测试不再碰真库）+ `test_api_v2.py`（21 个测试覆盖全部 9 接口，含 fixture/参数化） | 学员答"1 和 2"；学情：内置函数已会，真正欠的是算法与工程化 | `交互演示/算法①-哈希表查找.html`、`样例代码库/two_sum.py`、`conftest.py`、`test_api_v2.py` |
| 2026-09-20 | 新增**交互演示**目录与第一张卡（JOIN 配对过程） | 抽象概念文字讲不清 | `交互演示/JOIN配对过程.html` |
| 2026-09-20 | 新增**样例代码库**（len / range / enumerate / sql_join） | 学员自述"函数用得太少所以忘" | `样例代码库/*.py` |
| 2026-09-20 | 新增**打印资料 + PDF 流水线** | 需求：可打印、提前预习、对抗遗忘 | `打印资料/`、`tools/html2pdf.py` |
| 2026-09-20 | 第 3 课教案（数据模型 + JOIN）就绪，**尚未实际上课** | 卡点 P-009 | `第3课-数据模型与JOIN.md`、`第3课-微信群演示.py` |

---

## 三、待办与卡点（写清卡在谁身上）

| # | 待办 | 卡在谁 | 状态 |
|---|------|--------|------|
| T-01 | **第 3 课实际上课** + 填附录 C 登记表 + P-009 转 ✅ | 学员（约时间） | 🔄 **①–④ 已上（2026-09-23 晚）**；**⑤ 换皮回方剂库 + ⑥ 你讲我听 待下次（约 20 分钟）**；附录 C 与 P-009 收口等 ⑤⑥ 上完 |
| T-02 | D8 作业：选修课报名三张表 + JOIN，交上来批改 | 学员 | ⏳ 未开始 |
| T-03 | 下周逐日计划（要按 D1–D8 的卡点栏重出） | 导师（等卡点） | ⏳ |
| T-04 | ~~给"学员最常忘的函数"出交互卡片~~ → **已作废（2026-09-21）**：学员明确"内置函数不用教学，我明白"。模板与已有函数卡保留备用，**不再新增、不再排课** | 学员 | ❌ 关闭 |
| T-05 | ~~第 0/1 课卡片要不要转 HTML~~ → 同上，不作（学员不需要函数层教学） | — | ❌ 关闭 |
| T-12 | **AI Agent 学习线**（教材：`github.com/bojieli/ai-agent-book`）—— 路线已定：C（本地消融，**已完成**）→ A（第 1 章正文 + 实验 1-1，**实验已跑通，正文待读**）→ B（第 3 章 BM25/混合检索，接现有 RAG 的 87%） | 学员 | 🔄 开线中，路线图见 `打印资料/AI-Agent学习路线图.pdf` |
| T-13 | 可给原仓库提的两个 Issue（`agent.py` 缺 `import openai`；ollama `localhost` 在 IPv6 优先的 Windows 上连不上） | 学员定（要不要提） | ⏳ |
| T-09 | **算法 150 题（LeetCode Hot 100 为主）** —— 阶段 A 的**主目标**，看板 2.1 记 ⬜ 未开始 | 学员 | 🔄 **已起步**：模式① 哈希表（卡 + demo 就绪），待学员做题 |
| T-10 | **pytest 9 接口全绿** —— **已达成**（2026-09-21：27 passed，覆盖全部 9 接口，fixture 隔离） | — | ✅ 完成（看板 2.1 待更新为 ✅） |
| T-11 | 第 4 课/第 5 课是否上课（函数与参数已备教案；作用域与模块未做） | 学员定 | ⏳ 待定 |
| T-06 | `sql-notes.md` 第八节数字与库不符（笔记 37 味/48 条 vs 实测 33/49） | 学员自己数 | ⏳ 刻意留作教学素材，**AI 不代改** |
| T-07 | ~~表 1.3「编程主线每周可投入」仍空缺~~ → ✅ **2026-09-22 补齐：1.5–2 小时/天**（学员答）。⚠️ 与英语线是否重叠仍未澄清 → 教练按「含英语」的保守口径排计划，下周用实际数据校准 | 学员 | ✅ 完成（空了一个月） |
| T-08 | 2 周内 3 行业探索（P-011） | 学员 | 🔄 |
| T-24 | 🔴 **修复「1-3-7-30」复习机制**（P-015）—— 第 3 课当场暴露：09.06 学的 404 三步排查法，17 天后**完全想不起来**；账本 09-14 就记过「复习日程表逾期」，**空转了 9 天没人管** | 教练（排日期 + 落到看得见的地方） | 🔴 **未开始 —— 本次课后第一优先** |
| T-25 | 现有 **13 张暗色卡片**要不要统一换成新的「米黄方格纸」皮？（2026-09-23 学员已选：**先不做**，只做底层 + 模板 + 全局图） | 学员定 | ⏸️ 挂起（等他看完新模板再决定） |
| T-26 | 第 3 课 **⑤⑥** 必须带他撞一次 **id 重叠的假数据坑**（`prescriptions.db` 里 `prescriptions.id` 与 `ingredients.id` 都从 1 开始，配对准写反会返回 20 行"看着合理"的假数据） | 教练（下次课） | ⏳ 教案里已有该实验（`第3课-微信群演示.py` 演示 4） |
| **T-14** | 🆕 **项目立项**（路线 v4 阶段 A 主目标）：定载体 + 建仓库 + README 三段（解决什么问题/给谁用/怎么算成功） | 学员 | ⏳ **死线 2026.10** |
| **T-15** | 🆕 **每周一更新 `每周目标.md`**：重写本周三件事 + 把上周连同结果归档到 `周目标存档/YYYY-Wxx.md` | 教练（每周一） | 🔄 **机制已建立**（首个 = W39，2026.09.22 建） |
| **T-16** | 🆕 **考研保险的底子盘点**：① 安中医 2026 硕士专业目录是否含计算机/信息方向（`[未验证]`，见看板 4.5）② 学员的高数/线代/概率/操作系统/组成原理课**当前学得怎么样**（这是考研线唯一真实成本所在） | 学员（① 我可代查） | ⏳ 2028.03 前弄清即可，不着急 |
| **T-17** | ✅ **已发出（2026.09.23 学员确认）** —— 从 09-18 接到任务到发出**历时 5 天，其中最后 4 天是"做好了但没发"**。发出前补齐三样：① 数据集重建为**真实来源**（UCI #936 NPHA 714 条 + UCI #427 OlderActivity 52,482 条 + 武汉市民政局武民政〔2024〕6号 37 条）② **修掉一个潜在缺陷**（`combine_by_cost` 缺去重，人造用例复现 → 修好，真实数据零副作用）③ 导出两版材料 + 汇报措辞把「数据源找不到」改成「**我找到了更好的替代源**」 | 学员 | ✅ 完成 |
| **T-18** | ✅ **已决（学员 2026.09.22）：并行 —— 两条独立的线**（物联网线当毕设/导师线，自建 AI 应用当求职作品线）。~~并行有代价：留给技术三线只剩 ≈5–9 h/周~~ → 🔴 **该数已修正**：学员澄清 1.5–2 h/天**不含**英语 ⇒ **技术三线可用 10.5–14 h/周，账够**（见 T-20）。纪律保留两条：① **按 1.5 h/天（下限）排量** ② 同一周只保一条「主轴」（判据 = 谁的"停一天有人问"），但定位已由"生存必需"下调为"**效率手段**"。详见 `learning-roadmap.md`「🔵 第二条线」 | 学员已答 | ✅ 完成 |
| **T-19** | 🆕 **毕设位占坑**：导师说这套工作「可以是你的本科毕设」；毕设选题一般**大三下走学院流程**（2028.02–07）→ 须在 **2028.03 前**做出可见成果 | 教练排 + 学员执行 | 🔄 已记入路线 |
| **T-20** | ✅ **已澄清（学员 2026.09.22）：1.5–2 小时/天「不含英语的 45 分钟」** ⇒ 技术三线可用 **10.5–14 h/周**，而 W39 需要 7.75–10.25 h → **够用**；**上一版那笔「超预算 / 并行可能直接崩」的警告据此撤回**。纪律改为：**按 1.5 h/天下限排量 + 余量不填满** | 学员已答 | ✅ 完成 |
| **T-21** | 🆕 **备用方案：若将来真要物理合并 `E:\物联网` → `E:\项目学习`，第一步必须先写 `.gitignore`** —— 现成方案：4 个敏感件移进 `物联网\_私密\` 做**整目录** ignore（不靠 4 条易被误删的文件名规则）+ `物联网\demo\_raw\` ignore。**先保护、后移动，顺序不能反。** 另需处理撞名：两边都有 `时间线账本.md`（建议并入子目录而非摊平） | 学员（合并时） | ⏳ **本轮未执行**（学员选了不物理合并） |
| **T-22** | ⚠️ **待确认（可能要能向导师解释）**：`combine_by_priority` 的 `used` 是「**按服务类型**去重」（`used.add(s["服务类型"])`），**docstring 里没写这一条**；而刚修的 `combine_by_cost` 是「**按服务 ID**去重」。⇒ **两条规则的"去重粒度"不一致**。导师若问「为什么 A 按类别、B 按 ID」，要有说法（A = 类别级约束，避免同类服务重复；B = 只保证同一个服务不出现两次）。**要不要统一，学员定** | 学员 | ⏳ |
| **T-23** | 🔴 **等导师回复** —— 回复一到**立刻更新 `每周目标.md` 的 🔵 段，并可能重排本周主轴**（他的反馈可能改变优先级，例如让你先补「动态感知」）。**若 3 天无回复，主动跟进一句**（他原话：「有什么疑问或不懂的直接跟我沟通」） | 学员（我提醒） | ⏳ **进行中** |

---

## 四、踩过的坑（W = 坑；每条都要能防复发）

| 编号 | 日期 | 坑 | 怎么发现的 | 怎么修的 |
|------|------|----|-----------|----------|
| **W-01** | 2026-09-20 | **把"HTML 交互"和"Markdown 卡片"当成二选一，否掉了 HTML** | 学员直接指出：陌生函数就该做成可调参的 HTML | 改成"按用途分"（见第一节表）；v2 文档里也已标注此判断作废 |
| **W-02** | 2026-09-20 | 教案初稿写「`ON` 写错必然空结果，且不报错」——**断言错误** | **实跑** `p.id = pi.ingredient_id`：返回 **20 行伪正确数据**（因 `prescriptions.id` 与 `ingredients.id` 都从 1 开始，id 空间重叠） | 教案改为两种真实后果对照表（不重叠→0 行；重叠→伪正确）+ 加"诊断三步"；`第3课-微信群演示.py` 演示 4 专门复现 |
| **W-03** | 2026-09-20 | 交互卡片"演示 4"初版**逻辑不成立**（我把 person id 设成 7/9，却写 gid 会撞上） | 写完自己复核逻辑时发现 | 演示时把 persons 重排成 1/2，并**实跑确认**才写进输出 |
| **W-04** | 2026-09-20 | 交互卡片关键词校验用 `/on/.test(text)` → `SELECT * FROM persons` 里的 "pers**on**s" 被误判"有 ON ✅" | 读代码复核 | 改成 `words.includes("on")`（按独立单词匹配）+ 空输入单独提示；Playwright 实测三种输入验证 |
| **W-05** | 2026-09-20 | 样例代码库里在 **f-string / 双引号字符串里嵌了双引号** → SyntaxError（3 个文件全中） | `ast.parse` 批量语法检查 | 中文内容改「」括号；改完全部 `ast.parse` + 实跑通过 |
| **W-06** | 2026-09-20 | Edge headless 截图/打印 PDF 时，用 `E:/a/b` 这种**正斜杠路径 → 静默不产出文件**，退出码还是 0 | 第一次截图目录里没文件 | 改传 Windows 反斜杠路径（脚本里已固化）；`html2pdf.py` 里还加了"产出后检查文件存在+大小"而不是只看退出码 |
| **W-07** | 2026-09-20 | 改看板表 2.3 时，用 edit 把「第 2 课 FastAPI」那行**整行替换掉了**（内容丢失） | 回头读文件时发现行不见了 | 立刻补回原行；预案：改前已备份 `教学进度看板.md.bak-20260920-第3课教案`。**教训：做"追加"时 old_string 只取行尾锚点，别含整行** |
| **W-08** | 2026-09-20 | 验证命令里 `cmd | tail -3` 之后读 `$?` → 读到的是 tail 的退出码，**掩盖了真实失败** | 截图没生成但显示 exit 0 | 改成重定向到日志文件再 `cat`；或读 `PIPESTATUS` |
| **W-09** | 2026-09-20 | 控制台中文输出是 GBK 乱码（`ÿ�׷`），一度看不出查询结果 | 第一条 sqlite 查询输出全乱 | 脚本内 `PYTHONIOENCODING=utf-8` + **输出重定向到文件再用 read 工具读**（不要相信控制台渲染） |
| **W-10** | 2026-09-20 | 看板/账本**副本文件被 glob 当正文**（`教学进度看板.md.bak-*` 出现在文件清单里） | glob 结果 | 无实际危害，但读文档时要认准不带 `.bak-` 的才是正文 |
| **W-11** | 2026-09-20 | headless 截图时**把中文文件名放进 shell 变量再拼路径** → Edge 静默产出错名文件（`_shot-_卡片模板.png`），且下一条 `ls 交互演示/*.png` 的 glob 还骗我说"文件不存在" | 在目录里逐个核对时发现 | ① 截图输出名**只用 ASCII**，路径显式写全 ② 不轻信单次 `ls`/glob 结果，用整目录列表复查 ③ 用 `sha256sum` 确认两张图不是同一张 |
| **W-12** | 2026-09-20 | **同一个 bash glob 假阴性骗了我三次**（`ls 交互演示/*.png`、`ls 交互演示/shot-v*.png`、`ls E:/Temp/vcard-shot*.png` 都说"文件不存在"，而文件其实都在；`shot-v9.png` 也是先"不存在"后被发现） | 用 `ls -la 目录/`（整目录）或 pwsh `Test-Path` 复查时发现 | ① **不用 `ls 通配符` 判断文件是否存在**，改用 `ls -la 目录/` 或 pwsh `Test-Path` ② 生成文件的验证优先用能返回真实退出码的方式（如 pwsh `Start-Process -PassThru` + `ExitCode` + 文件大小） |
| **W-13** | 2026-09-20 | **交互测试自己数错点击次数 → 报出假 bug**：JOIN 卡动画"每 3 次点击一轮循环"，我按 6/8 次点击取样，正好落在 `phase=0`（重来）那一步，于是读到空结果表，误判"LEFT JOIN 坏了" | 拦截 `showResult` 打印真实入参后发现 `len=1` 且表格已渲染红行 → 代码本来就是好的 | ① 测状态机不要"数点击"，要**轮询状态直到终态**（如 `while(st.phase!==3) click()`）② 怀疑 UI 时先**拦截函数看真实入参**，再决定改不改代码 |
| **W-14** | 2026-09-21 | **"揭晓"按钮永远说"先填一个数"**（range / enumerate 两张卡）：处理器**先调 render() 再读输入框**，而 render() 的职责包含"清空上一轮猜测" → 用户填的数字被自己清掉 | Playwright 复现：设值后点击，值变空；给 render 加计数后定位到 `render#1 @guess="4"` 之后值即消失 | 铁律：**读用户输入必须在任何重绘之前**（先取值 → 再 render → 再比较）。已在两张卡按此修好并复测（对/错/空输入三种分支） |
| **W-15** | 2026-09-21 | **速查卡整页 JS 崩掉**：数据数组用单引号字符串，里面又写了 `['a','b','c']` → 语法错误，整页 `render()` 从未执行（表格空白） | 用 `node --check` 抽出 `<script>` 段做语法检查 → 精确报出 `SyntaxError: Unexpected identifier 'a'` | ① 中文说明里的引号统一用「」② **每张交互卡改完都跑一遍 `node --check`**（8 张卡已全量通过）——这比"打开浏览器看"更快更准 |
| **W-16** | 2026-09-21 | **同一个"中文引号嵌字符串"错误复发**：`样例代码库/two_sum.py` 里 `print("…同一个"用字典换值"的模式…")` → SyntaxError（W-05 的翻版，Python 侧） | 按纪律跑 `python 样例代码库/two_sum.py`，立刻报 `SyntaxError: invalid syntax` | 铁律：**中文里要引号就用「」**；并且**每个新建/修改的 .py 都要先 `ast.parse` 再实跑**（不能只看"写完了"） |
| **W-17** | 2026-09-21 | **交互卡里"预设的语义相似度"写得太死**：只认"整词互相包含"，导致"发汗""睡不好脸色差"这类问法在第③档返回 0 条（演示效果直接被削弱） | Playwright 逐条试 6 种问法 + 单字边界，发现两条 0 条 | 改成**字符重叠度 / 较短词长**（仍标注"本页是近似值，真实项目用 embedding 模型"）；复测 6 种问法排序正确、单字不误报 |
| **W-18** | 2026-09-21 | **"零依赖伪向量"幻想**：一度打算用「字 n-gram TF-IDF」代替 embedding 做语义检索 | **实跑实测**：`免疫`/`体虚乏力`/`增强体质`/`免疫力差` **全部 0 条命中**（只在词完全相同时才有效） | 结论：**语义检索没有免模型的捷径**，必须上 embedding（本机已装 Ollama + qwen2.5:3b，正在补 bge-m3） |
| **W-19** | 2026-09-21 | **RAG"装死"**：检索完全正确（四物汤 0.606 排第一），但模型回答"资料里没有提到" | 用同一份资料跑 A/B/C 三版提示词对照 → 严格版（"没信息就直说"）装死；鼓励版正确回答并引用；极简版只会抄资料 | 提示词规则改成"**资料相关的就用来回答 + 只在整段末尾单独一行写「依据：[编号]」+ 确实无关才说没有**"；复测 4 个问题全过（含"能治癌症吗"→ 正确拒答）。**教训：提示词过防御 = 模型变懒，这不是模型能力问题** |
| **W-20** | 2026-09-21 | **出题时把测试用例的期望值算错了**：`two_diff([5,1,3], 2)` 我写期望 `[0,2]`，实际按"从前往后扫第一个匹配"应得 `[1,2]`（5-3 和 3-1 都等于 2，**题目本身有两个合法解**） | 交付前用参考答案跑判分器做自检（不是空跑，而是"塞入正解看是否全绿"），一下暴露 2/4 个用例期望值错 | ① 修正期望值为 `[1,2]`，并在题目说明里写清"多解 + 判分顺序敏感" ② **新增铁律：任何判分器/测试文件交付前，必须先用正确解跑一遍确认全绿**（否则学员写对了却被判错，比不判还糟） |
| **W-21** | 2026-09-21 | **规格漂移害学员来回改**（严重）：`two_diff` 的"谁减谁"我**没在题目里写死**，第一版正解用 `need = x ± k`（找 前−后=k），我给的提示却引导"两种方向都想"，学员改成 `need = k - x`（找 后−前=k）后，**6 个用例全红** —— 但学员两次都没写错逻辑，是我在两个相反规格之间来回换 | 学员说"ok了"后我复测，发现 15/22（比上次 19/22 更差）→ 裸跑 + 打印函数源码缩进，逐条心算才定位到"方向反了" | ① **题目规格必须一次写死**（现在 docstring 明确"前减后=k"，并写明两个方向都要查的理由与例子）② 判分改用**校验函数**（`_valid_pair`：不同位置 + 差对得上即算对），多解题不再绑死某个数组 ③ 修完**用权威正解跑一遍确认 23/23** 才交付 |
| **W-22** | 2026-09-21 | **凭空断言"某用例是错的"**：我看了学员 `first_unique` 的代码（`for i in s` 遍历原串），却断言 `first_unique("bbac")` 应为 `'c'` —— 实际上 "bbac" 里 **a 只出现一次**，正确答案就是 `'a'`，学员代码本来就对 | 裸跑该用例 + 打印它数出的 `seed={'b':2,'a':1,'c':1}`，才发现是我读字符串时看错了 | **提"你的代码错了"之前必须逐条心算/实测那个用例**；本题的正确反例是 `"aab"`（b 唯一）而不是 `"bbac"`。**教训：指出别人错之前，先证明自己是对的** |
| **W-23** | 2026-09-21 | **超时护栏用了 Unix 专有的 `signal.SIGALRM`** → 在 Windows 上 `AttributeError`，25 个用例全部误报失败 | 加完护栏跑一次，满屏 `module 'signal' has no attribute 'SIGALRM'` | 改成**线程 + `join(timeout)`** 的跨平台实现（死循环线程设为 daemon，脚本退出时自动结束）。**教训同 W-06/W-11：写"环境相关 API"前先想清楚在不在本机可用** |
| **W-24** | 2026-09-21 | **直接跑学员代码 → 撞上死循环**：`max_area` 里 `while i < j` 但两个变量从不变化，判分器跑满 120 秒被我方的 timeout 杀掉，两次工具调用被中断，学员体验很差（原话"你怎么老是卡住"） | 学员反馈 + 用探针逐函数试（`two_sum_sorted` 正常返回、卡在 `max_area`）才定位 | ① 立规矩：**跑之前先"静态三查"**（循环变量是否推进 / 终止条件 / 有没有破坏题目前提），见第五节 0.5 ② 所有跑他人代码的执行器**必须带超时护栏** ③ 探针式排查要"每步写盘"，卡死也能看到卡在哪一行 |
| **W-25** | 2026-09-21 | **Ollama runner 崩了却继续产出"数据"**：本机 RTX 5060 Laptop 8G 日常已用 5.4G，Ollama **默认给模型开 32768 上下文**，KV cache 撑爆显存 → 跑几十次后 `model runner has unexpectedly stopped`。**最坑的是崩之后模型照常返回内容，但全是垃圾**（每道题都答 23），差一点被我当成实验结论写进卡片 | 可疑（摄影题真值 61 却稳定答 23）→ 直接 `curl .../api/ps` + 单发一次最小请求，拿到原文 `model runner has unexpectedly stopped, this may be due to resource limitations` | ① 请求里加 `"num_ctx": 4096`（实测加完 30 次突发调用零失败）② **看到"过于一致的怪答案"要怀疑环境，不要怀疑模型玄学** —— 崩后的假数据比报错危险得多 |
| **W-26** | 2026-09-21 | **题目答案碰巧等于模型的"默认猜测" → 该题在该组失效**：消融实验里围棋真值是 23，而「砍掉工具结果」组模型拿不到任何数据，却 **5/5 次全答 23** —— 连真值 61 的摄影题也答 23。说明 23 是它没信息时的默认猜测，那一格是"蒙对"，不是有效信号 | 逐轮打印每组的原始回答（不是只看总分）才发现同一数字反复出现 | ① 把围棋真值改成 31，那一列随即老实变成 0/5 ② **出题必问一句："这题答对，是真会，还是可能蒙对？"** ③ 汇报结果要带**每一格的原始回答**，只报总分会把蒙对藏起来 |
| **W-27** | 2026-09-21 | **`html2pdf.py` 用 `Path.as_uri()` 让 Edge 静默不出文件**：`file:///E:/%E9%A1%B9...` 这种编码正斜杠 URL，Edge headless **退出码 0、不报错、也不产出任何文件** → 极易误判成"生成好了"。同时轮询只等 6 秒，大 PDF 实际要 10 秒以上，把**已经生成好**的文件报成"失败" | 手动保留 stderr 跑 Edge + 用 Python 精确控制参数，四种 URL 写法对照：反斜杠形式 OK（525KB）、`as_uri()` 与正斜杠形式均静默失败 | ① URL 改用 `"file:///" + str(path)`（反斜杠原样）② 轮询放宽到 45 秒并确认**文件大小稳定**再算成功 ③ 铁律重申：**外部命令"退出码 0"不等于"做出了东西"**，必须验产物存在且大小合理（同 W-06/W-11） |
| **W-28** | 2026-09-21 | **书里 `--provider ollama` 在 Windows 上开箱即坏**：注册表把 ollama 地址写死 `http://localhost:11434/v1`，本机 `localhost` 解析到 IPv6 `::1`，Ollama 的 `/v1` 在该地址上连接被重置（`WinError 10054`）。容易误判，因为 `curl http://localhost:11434/v1/models` **返回 200** | 用 SDK 直接对两个地址各打一次：`localhost` → APIConnectionError，`127.0.0.1` → 正常返回 | ① 跑书里实验前设 `OLLAMA_BASE_URL=http://127.0.0.1:11434/v1` ② **别用 curl 的成功给 SDK/别的 HTTP 客户端背书** —— 两者的地址解析路径可能不同 |
| **W-29** | 2026-09-21 | **书里代码的 bug 会把真错误藏起来**：`chapter1/context/agent.py` 只 `from openai import OpenAI`（没 `import openai`），但异常处理里用 `openai.APITimeoutError` → **API 一出错，异常处理器自己先崩**，看到的全是 `NameError`，真正的 IPv6 连接失败被完全掩盖（即 W-28 的成因被隐藏） | 报错栈里 `NameError` 出现在 `except` 行的求值阶段 → 判定是异常处理路径本身的问题，不是原始请求的问题 | ① 在本地副本补 `import openai`（原仓库未改）② 教训：**异常处理里引用的名字，自己也要保证一定存在**；否则"错误处理"会变成"错误放大器" |
| **W-30** | 2026-09-21 | **把教材克隆放进工作区 → 污染了自己的测试**：`_ref/ai-agent-book/`（书里 109 个实验的源码）自带一堆 `test_*.py`，pytest 默认递归整个工作区 → 收集到 10 个第三方测试的 collection error，**把本工作区自己的 27 passed 直接淹掉**（跑出来是"10 errors"） | 收尾跑 `pytest -q` 做回归，发现从 27 passed 变成 10 errors；报错路径全指向 `_ref/ai-agent-book/...` | ① 新增 `pytest.ini`，`norecursedirs` 加 `_ref _备份` ② **注意 `norecursedirs` 一旦显式写就覆盖默认值**，必须把 pytest 默认那串（`*.egg .* _darcs build CVS dist node_modules venv {arch}`）原样抄回去，否则 `.venv`/`build` 又开始被收集 ③ 教训：**往工作区里放"别人的代码"之后，一定要重跑一遍本工作区的测试** |
| **W-31** | 2026-09-21 | 🔴 **关键词偏差 → 把自己的测量误差当成市场结论**：用「大模型」抓岗位，全国 484 条里算法:应用 = 6:1，于是我写下"应用岗很少"。**实际上是我搜的词本身就指向算法岗** —— 换「AI应用」搜，光校招就有 173 条；「Agent」180 条。**市场没变，是我的取景框歪了** | 补抓全国数据做城市对照时，发现各城市比例高度一致（都是 6-9:1）→ 一致得可疑 → 反查才发现是关键词造成的，不是城市差异 | ① **结论必须带"我是怎么测的"**：写比例时同时写清关键词、平台、样本量 ② 看到"所有分组结果高度一致"要先怀疑测量方法，而不是先高兴 ③ 教训：**换个词再搜一遍，是检验数据结论最便宜的办法** |
| **W-32** | 2026-09-21 | **把"接口被封"误读成"数据为 0"**：BOSS直聘连发十几次请求后返回 `code:37 您的环境存在异常.`，我在批量脚本里只取 `zpData.html`（空串）→ 杭州/上海/深圳/北京全部显示"0 条"。**如果不去看 `code` 字段，就会得出"这些城市没有大模型岗位"的荒谬结论** | 觉得"四个一线城市全是 0 条"不合常理 → 打印原始 `code`/`message`，看到 `37 您的环境存在异常` | ① 抓数据前**先校验响应码**，非 0 一律报错退出，绝不当作空结果 ② 教训：**"没抓到"和"不存在"是两件事**，中间差着一个错误码 |
| **W-33** | 2026-09-22 | 🔴 **做「全局路线」时漏掉了一整条已有工作区线**：v4 路线（倒推链 / 项目前置 / 考研备选 / 每周目标）**通篇没有 `E:\物联网`** —— 而该线**早已登记在** `E:\dsh优化\时间线账本.md` 的**全工作区索引**里（第 50–70 行：`E:\物联网` = 毕设/导师任务/毕业论文，有自己的账本），并且它**有导师、可能直接是毕设、还挂着实习推荐**。若不补，v4 会把"项目"算成只有一个，进而把时间预算算错 | **学员主动说**「物联网的那个工作区也算是我的一部分，那个是我导师给的任务」 | ① **立规矩：写任何"全局路线/时间预算"之前，先读 `E:\dsh优化\时间线账本.md` 的全工作区索引，核一遍到底有几条线**（那里有 15+ 个工作区）② 本条已补：`learning-roadmap.md` 增「🔵 第二条线」、`每周目标.md` 增独立节奏段、看板 4.1/4.5 ③ 教训：**"我以为只有教这一条线"是记忆问题，不是判断问题** —— 索引就是为这个存在的，而我有索引却没读 |
| **W-34** | 2026-09-22 | **`delivery_check` 的 `numeric` 证据格式连错三次**：`result` 必须是**裸数字**（如 `"1"` / `"27"`）。我试过 ① 带说明的文本 `"10.5–14 h/周（不含英语…）"` ② `key=value` 形式 `"pages=1"` —— **两种都被拒**，报错 `numeric evidence needs a numeric result (e.g. minr=2.07, H=0.00)`。同时 `evidence` 必须是 `{items:[…]}` **对象**，传裸数组会报 `missing evidence items` | 本轮复跑交付门时连吃三次 FAIL | ① **`numeric` 项只放裸数字**；要带解释就改用 `run` 项（`result` 是自由文本）② `evidence` 一律写成 `{"items": [...]}` ③ 已知能通过的组合（本轮 15 项）：`numeric` 2 项（`"1"` / `"27"`）+ `run` 若干 + `page`/`image` 各带 `reviewed: true` + `external` 的 `target` 传**输出路径**（不是命令行）④ **先 `tools_help delivery_check` 再猜** —— 我猜了三次才去读 spec ⑤ 🔴 **`external` 的 `target` 必须是本地文件路径 —— 既不能传命令行，也不能传 URL**（2026-09-22 第二次踩：传 `https://github.com/...` 报 `external target missing`）→ URL/命令这类信息写进 `result` 文本里 |
| **W-35** | 2026-09-22 | 🔴 **差点把别人的本科毕业论文推上公开 GitHub**：学员要求「物联网工作区与当前工作区合并」。核查时发现 `E:\项目学习` 的远端 `hanli1001/study-roadmap` 是 **PUBLIC**（`gh repo view` 实测 `"visibility":"PUBLIC"`，`isPrivate:false`），而 `E:\物联网` 里有 **4 个文件不在任何 `.gitignore` 保护下**：同学**许文涛的整本本科毕业论文 docx（4.5 MB，含其姓名/学号/指导教师名）**、该论文全文提取（112 K）、导师的选题材料 docx（他明说是「**我的一个科研项目**」）、与导师的私人沟通草稿（20 K）。**合并后一次 `git add .` 就会全部公开** —— 学术不端 + 侵权 + 泄露导师 IP 三件事一起发生。另 `demo\_raw\` 还有 4.8 M 下载数据（可复现，不该进库） | 学员要求合并 → **我先查了两边的 git 状态 + 远端可见性，没有直接动手** | ① **立规矩：跨工作区合并前必须查三件 —— 目标是不是 git 仓库 / 远端是 PUBLIC 还是 private / 待并入的文件里有没有"别人的东西"** ② 本次结论：学员选「**只在计划层打通，物理不合并**」→ 敏感件本就在仓库外，**风险自然消失，无需 .gitignore 补丁** ③ 教训：**绝不能默认"这个仓库是私有的"** —— 查一下只要 3 秒，猜错是永久性的 ④ 通用形态：**凡是"把 A 合进 B"的请求，先问"B 会到哪儿去"** |
| **W-36** | 2026-09-22 | **用 `edit` 追加表格行时，拿"下一行的行首"当锚点 → 把两条 T 项缝成了一行**：我要在 T-21 后追加 T-22，`old_string` 取了 `\| **T-21** \| 🆕 **备用方案：若将来真要物理合并…` 这段**行首前缀**，结果 **T-22 的全文被塞进 T-21 的中间**，T-21 剩下的半截挂在末尾 —— 两条待办变成一个畸形行（差点永久丢一条待办） | 追加完顺手 `grep "T-2[12]"` 复查，**只匹配到 1 行**（应该是 2）→ 立刻定位 | ① 🔑 **锚点规则（W-07 的精确版）：追加新行时，锚点必须是「上一行的完整内容」或「上一行的行尾」；✅ 最稳的是拿表头+分隔线当锚点** —— **绝不能用"下一行的行首"**，用行首 = 把新内容插进旧行内部 ② **每次 `edit` 动过表格后，立刻数该行的字段分隔符 `\|` 个数**（本表应为 5 个），数目不对就是串行了 ③ 本次已修复，T-21/T-22 各自完整（各 5 个分隔符） |
| **W-37** | 2026-09-23 | 🔴 **`Lab.rail()` 忘了给宿主加 `.rail` 类 → 整套轨道样式静默失效**：定位、上边距、窄屏横滑全靠 `.rail` 这个选择器，而函数里只 `host.textContent=''` 后直接 append 子节点，**从没 `classList.add('rail')`**。后果两条：① 红旗标签压住上方正文 **6px**（我先前还以为是 padding 不够，去改了 CSS 的 padding —— **改错了地方**）② 手机上 `.rail-line` 的 620px 最小宽度**顶破整页**，横向溢出 **132px** | Playwright 在 390px 下逐元素量 `right > clientWidth`，列出来的前几个全是 `rail-line` / `rail-stops`，而且我把它们排除后溢出仍在 → 反查 CSS 选择器才发现类名根本没加上 | ① **JS 生成的组件，第一件事是把根类名加上**；类名漏了不会报错，只会让 CSS 静默失效 ② 🔑 **症状出现在 A，病因可能在 B** —— 我先改了 CSS 的 padding（无效），**证据（逐元素量溢出）指向的是类名**。以后先量再改 ③ 修完复测：390px 下整页溢出 **132 → 0** |
| **W-38** | 2026-09-23 | **grid 子项的 `min-width:auto` 把整页顶宽**：`.row2{grid-template-columns:1fr 1fr}` 里放 `<pre>`（长代码行）→ grid 子项不收缩，整页在 390px 下横向溢出 **53px**。`pre{overflow-x:auto}` **挡不住**，因为撑宽的是 grid 轨道本身 | 同上一轮的量法：排除已知容器后仍有 7 个元素溢出，全是 `.row2` 里的 `H3`/`PRE`/`DIV`/`.out`，右边缘一致停在 428（= 375+53） | ① **凡是把可滚动的宽内容放进 grid/flex 子项，必须给子项 `min-width:0`** ② 顺手加 `pre,.out,.player,.detail,.guess,textarea{max-width:100%}` 兜底 ③ 复测：溢出 **53 → 0** |
| **W-39** | 2026-09-23 | ⚠️ **又一次差点把"渲染伪影"当成 bug**（这已是第三次）：配对动画的令牌文字 `uid 7 → id`，在截图里那个 `→` **看着像个 `✗`**，我一度打算去换字形。**放大 3 倍实测**后看清：箭头字形是对的，是**连线从字上横穿过去**造成的错觉。⇒ 真正的问题不是字形，是**令牌压在数据上面**（`overlapArea` 实测 1079px²） | 把截图裁出来 ×3 放大看，而不是靠缩略图的印象 | ① 修法：同行的连线改成**拱形**（`bow=44`）+ 给表格加一条 **48px 的"走线道"空行**，令牌停在连线最高点而不是终点 ② 复测：令牌与**表头 0 重叠、与数据行 0 重叠** ③ 🔑 **与 09-22「文字发蓝」、09-23「疑似下划线」同源**：**缩略图里的"看起来不对"，先量再改** —— 三次里三次都是伪影或另有真因 |
| **W-40** | 2026-09-23 | **在 HTML 里写了 Markdown 的 `**加粗**`**：新写的两个页面里一共 **14 处** `**…**`，浏览器**原样显示星号**。第一版整屏截图里就露出来了（"**你任何时候打开它**"），是我看图时发现的 | 看第一轮整页截图，正文里明晃晃的 `**` | ① HTML 里没有 Markdown，**加粗只能写 `<b>`** ② 交付前跑一遍 `grep -c '\*\*'`（本轮 14 → 0）③ 这类错误的共同点：**我脑子里想的是"排版"，手上写的是"标记"** —— 检查清单里加一条"成品里不该出现的字面标记" |

---

## 五、教学法的核心决策（别再推翻重来）

### ⚠️ 0.5 跑学员代码之前，必须先做"静态三查"（2026-09-21 学员明确要求）

> 学员原话："**以后写完代码，先看代码的合理性再运行我的代码，不然老卡住**"。
> 背景：学员的 `max_area` 里 `while i < j` 但 `i`、`j` 从不变化 → 真死循环 →
> 判分器跑了 120 秒超时、两次工具调用被中断，学员体验很差。
> **成本对比**：静态三查约 5 秒；跑一次死循环 = 120 秒 + 中断 + 学员困惑。

**跑之前必须逐条过（5 秒能做完）**：

| # | 查什么 | 这次的实例 |
|---|--------|-----------|
| ① | **每个循环的变量，是否每轮一定推进？**（`+= 1` / 重新赋值写在哪一行？） | ❌ `while i < j:` 里既无 `i += 1` 也无 `j -= 1` → 死循环 |
| ② | **递归/重试有没有终止条件？** | （本次不涉及，同理适用） |
| ③ | **代码有没有改变题目的前提？**（排序 / 改写输入 / 改类型） | ❌ `heights.sort()` 丢掉了"线的位置"，而位置正是答案的一部分 |

**顺序**：先静态三查 → 可疑就**先指出、不跑** → 确实要跑则**必须带超时护栏**。
（护栏坑见 W-23：第一版误用了 Unix 专有的 `signal.SIGALRM`，在 Windows 上全线报错。）

---

0. **⚠️ 学员明确划出的边界（2026-09-21）**：
│   - **Python 内置函数不用教学** —— 学员原话："python内置函数不用教学，我明白"。`len/range/enumerate/语法速查` 那几张卡**只作备用速查**，不再继续扩充、不再作为教学内容排课。
   - **不要再把"内置函数详解卡"当成待办或成果来推进**（这是 2026-09-20~21 走过的弯路：一口气做了 4 张函数卡，学员其实不需要）。
   - 真正该投入的是**主线**。⚠️ **2026-09-22 更正**：主线**不再是**「算法 150 题」（已降为**每天 1 题的地板**），
     而是 **「自己写的 AI 应用 v1（2026.10 立项 / 2027.01 上线公网）」+ Linux 15 任务 + SQL JOIN**。见 `learning-roadmap.md` v4 阶段 A。
1. **素材红线**：中医数据只当作业载体，**禁止讲中医语义**；抽象关系换通用场景（微信群/选修课/游戏）
2. **一次 ≤3 个新概念**（认知负荷）；`LEFT JOIN`/`GROUP BY` 只作"见识"，不列为必会
3. **"我懂了" = 危险信号** → 必须闭卷检索验证
4. **验收四级**：L1 照抄讲得清 → L2 换皮独立跑通 → L3 能讲给别人听 → **L4 能自己造工具/卡片**
5. **进度的前置条件不再是"上课"**：微课单元 + 打印计划表必须能**脱离导师独立执行**
6. **数字必须实测**：写进教案/看板的任何计数（几味药、几行）都要现跑一遍并留输出文件

### 🆕 0.7 每周一更新机制（2026-09-22 学员要求建立）

> 学员原话：「**以后每周一更新，一周学习的要点集合，就是当前周，要完成的学习目标**」

| 项 | 规则 |
|----|------|
| **文件** | `每周目标.md` —— **滚动文件，永远只有当前这一周**（一个链接永远有效） |
| **更新时点** | **每周一**（下一个周一 = **2026.09.28**） |
| **归档** | 上一周连同结果 → `周目标存档/YYYY-Wxx.md`；空白模板 `周目标存档/_模板.md` |
| **篇幅** | **最多 3 件事**，写满 3 个不许再加 |
| **验收标准** | 必须**能被验证**：「学完 Linux」❌ ｜「5 个任务跑过 + 不看文档说出任务 3 的命令」✅ |
| **必带栏目** | ① 倒推链三个数（距 2027.03 / 2028.03 / 2029.06）② 上周结转 ③ 三件事 ④ 每天地板 ⑤ **本周明确不做** ⑥ 最低版 ⑦ 下周一要带回来的东西 |
| **上周 ❌ 的处理** | **不许原样再列一遍** —— 要么拆小，要么写清为什么换掉 |

**为什么必须有「本周明确不做」这一栏**：学员的缺口清单有 6 项，全都想做 = 全都做不完。
**不做清单比做清单更能防摊大饼**（v4 的默认不做项：PyTorch / Java / 前端 / 囤课）。

---

## 六、每次改完必须做的三件事（缺一不可）

```
① 备份：cp 原文件 原名.bak-<日期>-<改动名>      （write 是整体替换，出过事故）
② 实测：教案里的命令/SQL/脚本都跑一遍，输出落盘  （W-02/W-05/W-06 都是这么抓出来的）
③ 登记：看板相关表 + 本文件（变更记录/坑/待办） + 时间线账本补一条
```

---

*改动路线 v1 · 2026.09.20 · 会话 compact 后请先读这份文件，再读看板*
